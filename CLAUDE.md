# CLAUDE.md

> **Last updated:** 2026-06-24 · **Commit:** `469f71d` (worktree-p193-p202-open-core-iter) · **Generated by:** `/init-deep` (update mode)

本文件用于指导 Claude Code 在本仓库内的工作方式。**优先级高于 `~/code/CLAUDE.md`（那份描述的是另一个项目，不适用于此处）。**

## 项目概述

`auto_trade` 是基于长桥（Longbridge / Longport）OpenAPI 的自动化**区间交易**系统：

- 后端：FastAPI + SQLAlchemy 2.0 + SQLite，运行交易状态机、风控、订单执行、多渠道通知（Server 酱 + Webhook）、操作审计。
- 前端：Vue 3 + Vite + Element Plus + TypeScript（Hash 路由）。
- 经纪商 SDK：`longport`（行情订阅 + 下单）。
- 可选：DeepSeek LLM 顾问，建议并应用 `buy_low` / `sell_high` 区间。
- 离线：CSV 回测（`BacktestEngine`，不发真实订单）。

**部署假设：** 仅内网/受控网络使用。Docker Compose 默认将前端绑定 `127.0.0.1:${AUTO_TRADE_FRONTEND_PORT:-8080}`，API 经 Nginx 反代；`AUTO_TRADE_API_KEY` 在 `dev/test` 可留空，`prod` 必填。

## 目录结构

```
auto_trade/
├── backend/
│   ├── app/
│   │   ├── main.py                         # FastAPI 入口、lifespan、LLM 定时 cron
│   │   ├── config.py                       # pydantic-settings（AUTO_TRADE_* / LONGPORT_*）
│   │   ├── database.py                     # engine、init_db、SQLite _ensure_*（tracked_entries / audit_logs / session / notification_channels…）
│   │   ├── models.py                       # StrategyConfig、OrderRecord、TrackedEntry、TradeEvent、LLMInteraction、RuntimeState、AuditLog…
│   │   ├── schemas.py                      # Pydantic API schemas（含 AuditEventOut / NotificationChannel / 跨表 union EventPageResponse）
│   │   ├── runner.py                       # AppRunner：行情订阅、策略循环、WS 广播、_check_trading_session（层 A）
│   │   ├── api/
│   │   │   ├── strategy.py                 # /api/strategy、/api/status、/api/status/history
│   │   │   ├── trade.py                    # 订单、账户、事件（跨表 union）、/api/control/*
│   │   │   ├── credentials.py              # 加密凭证 CRUD + notification_channels
│   │   │   ├── llm_advisor.py              # LLM 区间 analyze/preview/status
│   │   │   ├── backtest.py                 # POST /api/backtest/{run,sweep,walk-forward,stress} + /runs (saved-run CRUD + compare)
│   │   │   ├── indicators.py               # GET /api/indicators（实时技术指标快照，只读）
│   │   │   ├── ws.py                       # WebSocket /ws
│   │   │   ├── auth.py                     # require_api_key（dev/test 可空；prod 必填）
│   │   │   └── deps.py                     # get_audit_logger / extract_actor DI helpers
│   │   ├── core/
│   │   │   ├── broker.py                   # BrokerGateway：quote(s)、candlesticks、下单 + _call_with_retry 分档
│   │   │   ├── market_calendar.py          # trade_day_for / is_trading_hours（US/HK 本地日，不含节假日）
│   │   │   ├── engine.py                   # flat/long/short 状态机
│   │   │   ├── risk.py                     # 日亏损、连续亏损；可注入 trade_day_provider
│   │   │   ├── backtest.py                 # 离线回测引擎
│   │   │   ├── notify.py                   # 兼容层 re-export
│   │   │   ├── notifiers/                  # NotifierInterface / MultiChannelNotifier / ServerChan / Webhook
│   │   │   ├── audit.py                    # AuditLogger：审计写表、actor_hash、IP 提取
│   │   │   └── credential_crypto.py        # RSA + AES-GCM（AUTO_TRADE_CREDENTIAL_KEY_PATH）
│   │   ├── platform/                       # P149–P172+P173–P182：策略插件 SDK + 统一事件流 + 组合/Paper/风控 + 研究级分析 + 机构级编排（OMS/持仓/构造/预热/账本/调度/MC/投影/多币种）
│   │   │   ├── __init__.py
│   │   │   ├── events.py                   # 统一事件基类与具体事件类型（bar/quote/fill/order/signal/risk/control）
│   │   │   ├── sdk/                        # Strategy Protocol、OrderIntent（含 stop/trailing/OCO）、StrategyContext
│   │   │   ├── context.py
│   │   │   ├── registry.py                 # 策略插件注册表与自动发现
│   │   │   ├── bus.py                      # 内存事件总线
│   │   │   ├── store.py                    # EventLog 持久化仓库
│   │   │   ├── runner.py                   # PlatformRunner（多 symbol、ExecutionClient、RiskEngine、IndicatorService、Universe、Scheduler、warmup）
│   │   │   ├── replay.py                   # EventReplayer 事件回放
│   │   │   ├── simbroker.py                # 简化回测撮合器（保留；runner 已改用 PaperBroker）
│   │   │   ├── execution.py                # P168：ExecutionClient Protocol + LiveExecutionClient
│   │   │   ├── paper_broker.py             # P152/P154/P155/P172：成交仿真（partial/止损/追踪/OCO/可插拔 FillModel + paper_orders 持久化）
│   │   │   ├── paper_order_state.py        # P152：Paper 订单状态机
│   │   │   ├── fill_model.py               # P172：SlippageModel/CommissionModel Protocol + 各实现
│   │   │   ├── oms.py                      # P173：中央订单管理 OMS（订阅 order/fill，状态查询）
│   │   │   ├── position_engine.py          # P174：持仓引擎（FLAT/LONG/SHORT 净额 + 翻转 + realized）
│   │   │   ├── portfolio.py                # P164：中央账户状态（cash/positions/NAV/realized）
│   │   │   ├── portfolio_config.py         # P151：组合配置 dataclass + 校验
│   │   │   ├── portfolio_allocator.py      # P151：目标权重 → 再平衡 OrderIntent
│   │   │   ├── portfolio_service.py        # P151：组合配置 CRUD
│   │   │   ├── portfolio_api.py            # P151/P157/P162：/api/portfolio/*（config/attribution/kill-switch）
│   │   │   ├── portfolio_risk.py           # P156/P156+：组合级敞口/集中度/相关性/回撤
│   │   │   ├── risk_engine.py              # P156：事件驱动风控引擎（可注入 Portfolio）
│   │   │   ├── portfolio_runner.py         # P161/P162：组合再平衡 + kill-switch + CRITICAL gate
│   │   │   ├── attribution_service.py      # P157：FIFO realized + unrealized 归因
│   │   │   ├── backtest_service.py         # P153：平台回测（含 Portfolio + analytics）
│   │   │   ├── analytics.py                # P163：绩效分析（Sharpe/Sortino/maxDD/Calmar/win-rate/profit-factor）
│   │   │   ├── analyzers.py                # P171：TradeAnalyzer/DrawDownAnalyzer/ReturnsAnalyzer
│   │   │   ├── benchmark.py                # P175：基准 alpha/beta/tracking-error/information-ratio/capture
│   │   │   ├── construction.py             # P176：组合构造模型（EqualWeight/RiskParity + weights_to_intents）
│   │   │   ├── sizers.py                   # P165：仓位定尺（FixedFractional/FullEquity/ATR）
│   │   │   ├── indicators.py               # P166：指标即服务（SMA/EMA/RSI/ATR）
│   │   │   ├── universe.py                 # P167：标的全集（Static/TopNByVolume）
│   │   │   ├── warmup.py                   # P177：历史预热 WarmupProvider（Lean SetWarmup）
│   │   │   ├── scheduler.py                # P179：调度器（every-N-bars + daily-at-time）
│   │   │   ├── data_catalog.py             # P169：历史 bar 目录 + 时间桶重采样
│   │   │   ├── optimizer_service.py        # P170：参数寻优（grid + walk-forward IS/OOS）
│   │   │   ├── smart_optimizer.py          # P188：智能寻优（准随机 + successive-halving 中位剪枝）
│   │   │   ├── montecarlo.py               # P180：蒙特卡洛稳健性（bootstrap + 分位 + 破产概率）
│   │   │   ├── projections.py              # P181：事件投影（NavProjection/DailyReturnsProjection + 引擎）
│   │   │   ├── cashbook.py                 # P182：多币种 CashBook（FX 聚合基币 NAV）
│   │   │   ├── transaction_service.py      # P178：交易账本（per-fill 持久化 + 查询）
│   │   │   ├── execution_algorithms.py     # P183：算法执行（TWAP/VWAP/Iceberg 拆单 + 注册表）
│   │   │   ├── factors.py                  # P184：因子库（momentum/volatility/meanrev）+ 横截面 IC
│   │   │   ├── strategy_combinator.py      # P185：多策略组合（加权 alpha 合流，实现 Strategy）
│   │   │   ├── brinson.py                  # P186：Brinson-Fachler 归因（allocation/selection/interaction）
│   │   │   ├── backtest_run_service.py     # P187：平台回测运行持久化 + 对比
│   │   │   ├── session_filter.py           # P189：交易时段过滤（pre/rth/post/closed）
│   │   │   ├── margin.py                   # P190：保证金/杠杆模型（FixedMarginModel + LeverageGuard）
│   │   │   ├── tearsheet.py                # P191：tearsheet 构建 + CSV/JSON 导出
│   │   │   ├── latency.py                  # P192：订单延迟模型（FixedLatencyModel）
│   │   │   ├── distributed_bus.py          # P193：跨进程事件总线（Transport Protocol + DistributedEventBus + Redis/Null 适配）
│   │   │   ├── order_book.py               # P194：L2 订单簿撮合（价格层 FIFO + 跨层 partial）
│   │   │   ├── continuous_contract.py      # P195：连续合约换月 + RATIO/BACKWARD/NONE 调整因子
│   │   │   ├── factor_research_service.py  # P196：因子研究仓库（factor_snapshots/ic_series 持久化 + IC 时序）
│   │   │   ├── strategy_isolation.py       # P197：多策略并发隔离（per-strategy Portfolio + 资本预留 + 聚合 NAV）
│   │   │   ├── signal_decay.py             # P198：在线信号衰减（ExponentialDecay 半衰期 + RollingWindowDecay）
│   │   │   ├── tca.py                      # P199：TCA 交易成本分析（滑点/佣金按维度归因）
│   │   │   ├── stress_scenarios.py         # P200：宏观压力场景库（equity_crash/vol_spike/corr_break/liquidity）
│   │   │   ├── overfitting.py              # P201：回测过拟合诊断（PBO CSCV + Deflated Sharpe Ratio）
│   │   │   ├── brinson_multiperiod.py      # P202：多期 Brinson 链接（算术 BHB + Frongello 几何 + residual 对账）
│   │   │   ├── covariance.py               # P208：协方差矩阵 + Ledoit-Wolf 收缩（sample/correlation/portfolio_variance）
│   │   │   ├── mean_variance.py            # P206：Markowitz min-var/max-Sharpe + efficient frontier + MeanVarianceModel
│   │   │   ├── black_litterman.py          # P207：Black-Litterman 投资组合（market_implied + views + posterior + BlackLittermanModel）
│   │   │   ├── hrp.py                      # P209：层次风险平价（quasi-diagonalize + recursive_bisection + HRPModel）
│   │   │   ├── risk_metrics.py             # P203：VaR/CVaR 历史+参数化（Gaussian quantile via Acklam） + portfolio_var + risk_metrics report
│   │   │   ├── drawdown_analysis.py        # P204：回撤分析（events/summary/rolling_calmar/underwater/acceleration）
│   │   │   ├── risk_ratios.py              # P205：完整风险比率族（Sharpe/Sortino/Information/Treynor/Modigliani/Omega + rolling_sharpe）
│   │   │   ├── pain_metrics.py             # P209：Pain/Ulcer Index + MAR/Kestner ratio
│   │   │   ├── fat_tail.py                 # P210：肥尾诊断（excess kurtosis/skewness/Hill/tail ratio/stable fit）
│   │   │   ├── regime.py                   # P213：市场状态识别（BULL/BEAR/SIDEWAYS：SMA 交叉 + ADX + 实现波动，RegimeModel 流式 + RegimeEvent）
│   │   │   ├── cpcv.py                     # P214：组合交叉验证分割器（purge+embargo+C(N,k) 枚举 + OOS 路径 + cpcv_pbo）
│   │   │   ├── style_analysis.py           # P215：收益风格分析（Sharpe 1992：纯 Python Lawson-Hanson NNLS + 等式约束 simplex）
│   │   │   ├── turnover_optimization.py    # P216：换手感知优化（subgradient + simplex 投影 + L1 上限，TurnoverAwareModel）
│   │   │   ├── risk_budgeting.py            # P217：凸风险预算/ERC（Spinu Newton + 对数障碍 + risk_contributions，RiskBudgetingModel）
│   │   │   ├── trade_excursion.py           # P218：MFE/MAE + 持仓周期分析（per-trade + percentiles + trades_from_fills FIFO）
│   │   │   ├── shortfall.py                 # P219：Implementation Shortfall TCA（Perold 1988：realized/opportunity/timing/fees）
│   │   │   ├── returns_analysis.py          # P220：收益日历热力图（monthly/yearly/weekday/streaks/monthly_returns_table）
│   │   │   ├── stress_report.py             # P221：压力场景报告聚合（场景 VaR + 最坏场景 + 资本充足率，StressReportBuilder）
│   │   │   ├── stability_analysis.py        # P222：walk-forward 参数稳定性（IS/OOS 退化 + 邻域稳定性 + 最优参数漂移）
│   │   │   ├── cointegration.py            # P223：协整与配对交易（Engle-Granger OLS 对冲比 + OU 半衰期 + z-score + Durbin-Watson）
│   │   │   ├── kelly.py                    # P224：Kelly 仓位定尺（binary/continuous Kelly + 分数 Kelly + 期望对数增长 + 破产概率）
│   │   │   ├── volatility_models.py        # P225：波动率预测（EWMA RiskMetrics + GARCH(1,1) + Parkinson 高低价）
│   │   │   ├── microstructure.py           # P226：微观结构（VPIN 等量分桶 + OFI 订单流不平衡 + Kyle λ 价格冲击）
│   │   │   ├── execution_cost.py           # P227：Almgren-Chriss 最优执行（风险厌恶轨迹 + 成本/风险 + 执行有效前沿）
│   │   │   ├── hawkes.py                   # P228：Hawkes 自激发过程（branching ratio + 递归对数似然 + 强度路径）
│   │   │   ├── historical_scenarios.py     # P229：历史场景压力（episode 库 + apply_scenario + 资本充足率）
│   │   │   ├── factor_risk.py              # P230：因子风险分解（Barra 式 wᵀBF Bᵀw + 特质 + per-factor 贡献）
│   │   │   ├── sensitivity.py              # P231：参数重要性（Saltelli 一阶/全阶 Sobol + 交互 + 排名）
│   │   │   ├── extreme_value.py            # P232：极值理论（POT + GPD MoM 拟合 + 尾部 VaR/CVaR 外推）
│   │   │   ├── causal_analysis.py          # P233：因果发现（Granger F 统计 + betai p 值 + PCMCI 偏相关滞后 + lead-lag）
│   │   │   ├── regime_hmm.py               # P234：隐马尔可夫状态（Baum-Welch 矩估计 + Viterbi log-scale + BULL/BEAR/SIDEWAYS 标签）
│   │   │   ├── copula.py                   # P235：Copula 尾部相依（Kendall τ 一致对计数 + Gumbel/Clayton MoM 拟合 + λ_U/λ_L 闭式）
│   │   │   ├── drawdown_forecast.py        # P236：回撤预测（underwater 期 + 恢复时分布 + Brownian 反射原理 E[M_h] + Acklam 分位）
│   │   │   ├── liquidity_metrics.py       # P237：流动性（Amihud illiquidity + Roll 价差 + Pastor-Stambaugh 流动性β + Corwin-Schultz 高低价）
│   │   │   ├── momentum_factors.py        # P238：动量/反转因子（Jegadeesh-Titman 横截面 + De Bondt-Thaler 反转 + Carhart + TSMOM）
│   │   │   ├── portfolio_decomposition.py # P239：组合分解（returns→factors OLS + 收益/方差贡献对账 + 因子方差归因）
│   │   │   ├── spa_test.py                # P240：SPA 检验（Hansen-White superior predictive ability + 确定性 stationary block bootstrap）
│   │   │   ├── execution_quality.py       # P241：执行质量计分卡（fill 统计 + 滑点分布 + 反转 adverse-selection + A/B/C/D 评级）
│   │   │   ├── diversification.py         # P242：分散度（effective N + Choueifaty-Coignard DR + 分散收益 + HHI 集中度）
│   │   │   ├── _math_utils.py             # P243：共享数学（norm_cdf/norm_inv Acklam/norm_pdf，无 scipy）
│   │   │   ├── options_pricing.py         # P243：欧式期权 BSM 闭式定价 + Greeks（Δ/Γ/ν/Θ/ρ/vanna/volga）
│   │   │   ├── implied_volatility.py      # P244：BS 隐含波动率（Newton+bisection）+ Gatheral SVI 5 参原始参数化拟合
│   │   │   ├── kalman_filter.py           # P245：线性 Kalman 滤波（Joseph 形式）+ RTS 固定区间平滑
│   │   │   ├── stochastic_processes.py    # P246：随机过程（GBM/OU/CIR/Merton-JD Euler 仿真 + 解析矩）
│   │   │   ├── stat_arb_signals.py        # P247：统计套利信号（距离法 spread + z-score 阈值 + 复用 P223 OU 半衰期）
│   │   │   ├── robust_statistics.py       # P248：鲁棒统计（MAD/winsorize/trimmed mean/Theil-Sen/Huber IRLS）
│   │   │   ├── bandits.py                 # P249：多臂 Bandit（ε-greedy/UCB1/Thompson Beta+Gaussian + regret）
│   │   │   ├── loess.py                   # P250：LOESS/LOWESS（Cleveland tricube + 鲁棒 bisquare 迭代）
│   │   │   ├── smart_order_routing.py     # P251：智能订单路由（多 venue 最优价 + tick 量化 + 拆单计划）
│   │   │   ├── vine_copula.py             # P252：Vine Copula（C-vine/D-vine 逐对 copula 构造 + AIC/BIC）
│   │   │   ├── factor_turnover.py         # P269：因子换手率（rank turnover + bucket retention + rank autocorrelation）
│   │   │   ├── factor_decay.py            # P270：因子 IC/RankIC 多 horizon 衰减 + half-life horizon
│   │   │   ├── factor_quantiles.py        # P271：quantile forward return + top-bottom spread + monotonicity
│   │   │   ├── ic_diagnostics.py          # P272：IC 时序诊断（positive ratio / t-like / cumulative drawdown）
│   │   │   ├── factor_data_quality.py     # P273：因子面板质量（coverage/missing/constant/outlier/stale）
│   │   │   ├── signal_persistence.py      # P274：信号持续性（autocorrelation decay + half-life + turnover proxy）
│   │   │   ├── strategy_quality.py        # P275：策略 SQN / expectancy / payoff / sample confidence
│   │   │   ├── regime_performance.py      # P276：按 regime 切片收益、波动、胜率、贡献
│   │   │   ├── strategy_diversification.py # P277：多策略相关矩阵、冗余对、分散化得分
│   │   │   ├── backtest_confidence.py     # P278：bootstrap CI + rolling Sharpe 稳定性 + fragility
│   │   │   ├── forecast_diagnostics.py    # P279：预测诊断（MSE/MAE/bias/directional accuracy/IC/bucket spread）
│   │   │   ├── triple_barrier.py          # P280：三重障碍标签（profit/stop/time）
│   │   │   ├── sample_uniqueness.py       # P281：样本唯一性与事件并发权重
│   │   │   ├── bar_builder.py             # P282：tick/volume/dollar research bar 构造
│   │   │   ├── factor_neutralization.py   # P283：market/group 中性化 + OLS residualize
│   │   │   ├── factor_tearsheet.py        # P284：因子 IC/quantile/turnover/quality 聚合报告
│   │   │   ├── feature_pipeline.py        # P285：白名单声明式特征管道（无 eval）
│   │   │   ├── signal_backtest.py         # P286：entry/exit 或 target position 数组快速回测
│   │   │   ├── rolling_tearsheet.py       # P287：rolling Sharpe/MDD/beta/alpha 多窗口报告
│   │   │   ├── portfolio_constraints.py   # P288：组合 exposure/turnover/group/capacity 约束诊断
│   │   │   └── api.py                      # /api/platform/*（含 P279-P288 ML 研究流水线端点）
│   │   ├── strategies/                     # 策略插件包（IntervalStrategy 首个插件）
│   │   │   ├── __init__.py
│   │   │   └── interval_strategy.py        # 区间策略插件化实现
│   │   └── services/
│   │       ├── trade_execution_service.py  # 下单、pending 对账、tracked 入场成本持久化、HK tick、层 B 时段守卫
│   │       ├── strategy_service.py         # update_config 返回审计 diff
│   │       ├── runtime_state_service.py
│   │       ├── daily_pnl_service.py
│   │       ├── credentials_service.py      # notification_channels CRUD + 脱敏 diff
│   │       ├── llm_advisor_service.py
│   │       ├── interval_application_service.py
│   │       ├── llm_interaction_service.py
│   │       ├── trade_event_service.py
│   │       ├── event_list_service.py       # 跨表 union（trade_events + audit_logs）分页
│   │       └── data_aggregator.py          # 真实 K 线 → LLM prompt / ATR / 布林带
│   ├── tests/                              # pytest（见「测试约定」），含 tests/platform/ 平台层测试
│   ├── alembic/                            # 历史迁移；运行时以 database._ensure_* 为准
│   ├── requirements.txt
│   └── requirements-dev.txt                # pytest / basedpyright
├── frontend/
│   ├── src/
│   │   ├── router/index.ts                 # /, /strategy, /history, /events, /backtest, /credentials, /lab
│   │   ├── api/                            # client.ts + 按域模块（含 lab.ts：experiments/indicators/performance）
│   │   ├── composables/                    # useDashboardData、useStatusStream…
│   │   ├── components/                     # PriceChart、PnLChart、BacktestChart
│   │   ├── views/                          # Dashboard、Strategy、TradeHistory、DecisionTimeline、Backtest、Credentials、Lab.vue
│   │   └── types/index.ts
│   └── cypress/e2e/                        # E2E（stub API）
├── docker-compose.yaml                     # frontend 默认 127.0.0.1:8080；backend 仅容器内网
├── docs/Roadmap.md
└── README.md                               # 完整 API 表与部署说明
```

## 构建 & 运行

### Docker（推荐，内网）

```bash
cp .env.example .env   # 填写 LONGPORT_*；API_KEY 可留空
docker compose up --build -d
# UI + API: http://<host>:8080  （/api、/ws 经 Nginx 反代）
```

### Docker 镜像（P23+）

- **后端** `backend/Dockerfile` 多阶段：`builder` 阶段（带 `build-essential`）→ 编 wheel 进 `/opt/venv`；`runtime` 阶段基于 `python:3.11-slim` 仅复制 venv + app 代码，无 toolchain。runtime 阶段 `apt-get install` 后立即 `rm -rf /var/lib/apt/lists/*`；build 阶段同理后 `apt-get purge build-essential`。
- 入口：tini 转发信号，exec `docker-entrypoint.sh`（alembic stamp + upgrade + `init_db()` + uvicorn）。
- `.dockerignore`：根目录 + `backend/.dockerignore` 双重排除 `data/`、`*.db`、`.venv`、`.pytest_cache`、`.coverage*`、`.git`、`*.md`、`docs/`、`node_modules`、`dist` 等。
- 前端 `frontend/Dockerfile` 已是 `node:20-alpine builder → nginx:alpine` 两阶段。

### 后端（本地开发）

```bash
cd backend
python3 -m venv .venv && source .venv/bin/activate   # 必须 Python 3.11+
pip3 install -r requirements.txt -r requirements-dev.txt
uvicorn app.main:app --reload --port 8000
```

### 前端（本地开发）

```bash
cd frontend
npm install
npm run dev        # http://localhost:3000，代理 /api、/ws → :8000
npm run type-check
npm run build
```

### 测试与类型检查

```bash
cd backend && python3 -m pytest tests/ -v
python3 -m basedpyright          # 需仓库根 pyrightconfig.json（本地可能 gitignore）
cd frontend && npm run type-check
```

### 覆盖率（coverage）

`pytest.ini` 已挂上 `pytest-cov`，运行 `pytest` 时自动生成 terminal 报告。`.coveragerc` 排除 `app/main.py` / `app/runner.py` / `app/database.py` / `app/config.py` / `app/api/ws.py`（这些是启动/事件循环粘合层，单测覆盖成本高）。当前基线：**2431 passed**（平台层 P149–P252 全部已并入，含 P203–P212 风险科学 + 投资组合优化 10 个模块，P213–P222 10 个新模块与 10 个新端点，P223–P232 第二批 10 个新模块与 10 个新端点，P233–P242 第三批 10 个新模块与 10 个新端点，及 P243–P252 第四批 10 个新模块与 10 个新端点：options_pricing/implied_volatility/kalman_filter/stochastic_processes/stat_arb_signals/robust_statistics/bandits/loess/smart_order_routing/vine_copula，覆盖率 ~90%；已知 4 项 pre-existing 浮点边界失败：`test_fat_tail.stable_fit_handles_constant_returns` / `test_risk_ratios.sharpe_zero_when_constant_returns`（常返序列 std≈1e-18 ≠ 0）+ `test_config` 两项 env-file 顺序敏感，与本批无关）。提交 PR 时如改动某模块覆盖率下降超过 5%，请在描述里说明。

## API 速查

完整说明见 `README.md`。主要前缀均为 `/api`（回测为 `/api/backtest`）。

| 域 | 代表路径 |
|----|----------|
| 平台插件 | `GET /api/platform/strategies`（已注册策略插件列表，含 parameter_schema） |
| 平台回测 | `POST /api/platform/backtest`（任一已注册策略在 K 线上跑 PaperBroker，返回 equity/fills/positions/realized_pnl/analytics；422 缺字段、404 未知策略；带 API key） |
| 平台绩效分析 | `POST /api/platform/analyze`（权益曲线计算 Sharpe/Sortino/maxDD/Calmar/收益序列；可选 `benchmark_equity` 附 alpha/beta/capture；422 缺 equity_curve；带 API key） |
| 平台 tearsheet | `POST /api/platform/tearsheet`（同 `/backtest` 入参，构建完整 tearsheet：summary/equity/returns/drawdown/trades；`format=csv|json`；422/404；带 API key） |
| 平台回测运行 | `POST /api/platform/backtest/runs`（命名保存回测结果）、`GET /backtest/runs`（列表）、`GET /backtest/runs/{id}`（详情）、`GET /backtest/runs/compare?ids=`（对比）；带 API key） |
| 平台蒙特卡洛 | `POST /api/platform/montecarlo`（对每笔交易 PnL 做 bootstrap 重采样，返回最终 PnL 分位/破产概率/分位路径；422 缺 trade_pnls；带 API key） |
| 平台交易账本 | `GET /api/platform/transactions`（`transactions` 表 per-fill 查询：`symbol`/`since`/`until`/`limit`；带 API key） |
| 平台参数寻优 | `POST /api/platform/optimize`（grid/walk-forward 搜索策略参数，按 Sharpe/PnL 排名 + IS/OOS 拆分；422/404；带 API key） |
| 平台历史 bar | `GET /api/platform/bars?symbol=&resolution_minutes=&limit=`（从 `event_log` 拉历史 bar 并按时间桶重采样；422；带 API key） |
| 平台快照 | `GET /api/platform/snapshot`（运行中 `PlatformRunner` 的 mode/symbols/持仓/未结 paper 订单；runner 未启用 404；带 API key） |
| 平台事件 | `GET /api/platform/events`（`event_log` 分页查询：`symbol`/`since`/`limit`；`limit∈[1,10000]` 否则 422；带 API key） |
| 平台回放 | `POST /api/platform/replay`（确定性回放窗口 BarEvent 到新 runner，返回 reconstructed_positions；带 API key） |
| 平台风险指标 | `POST /api/platform/risk-metrics`（VaR/CVaR + drawdown + pain + tail + ratios 统一入口；`returns` 或 `equity_curve` 入参；带 API key） |
| 平台组合优化 | `POST /api/platform/portfolio-optimize`（`method`: `min_variance`/`max_sharpe`/`hrp`/`black_litterman`/`turnover`/`risk_budgeting`；`returns_panel` + 可选 `market_weights`/`views`/`prev_weights`/`budgets`；带 API key） |
| 平台市场状态 | `POST /api/platform/regime`（BULL/BEAR/SIDEWAYS：SMA 交叉 + ADX + 实现波动；`closes` + 可选 `highs`/`lows`；422 缺/空/过短；带 API key） |
| 平台 CPCV | `POST /api/platform/cpcv`（组合交叉验证枚举：`n_samples`/`n_groups`/`k_test`/`purge`/`embargo` → splits + summary + oos_paths；422 非法配置；带 API key） |
| 平台风格分析 | `POST /api/platform/style-analysis`（Sharpe 1992：`returns` + `factor_returns` + `constraint` none/sum_le_one/sum_eq_one → weights/R²/tracking_error；422 缺/短/非法；带 API key） |
| 平台 MFE/MAE | `POST /api/platform/trade-excursion`（per-trade MFE/MAE + holding_bars：`trades` + `bars` → per_trade + summary；422 缺参；带 API key） |
| 平台执行缺口 | `POST /api/platform/shortfall`（Perold implementation shortfall：`order` + `fills` → realized/opportunity/timing/fees/total/bps；422 缺 order/未知 side/close 缺价；带 API key） |
| 平台收益日历 | `POST /api/platform/returns-calendar`（`returns` 或 `equity_curve` + 可选 `dates` → monthly/yearly/weekday/streaks/summary/monthly_table；422 缺；带 API key） |
| 平台压力报告 | `POST /api/platform/stress-report`（`positions` {symbol:[qty,price]} + 可选 `betas`/`base_nav`/`confidence_levels`/`capital_buffer` → per-scenario PnL + worst + scenario_var + 资本充足率；422 缺/空；带 API key） |
| 平台参数稳定性 | `POST /api/platform/stability`（walk-forward `wf_results` + `metric`/`higher_is_better` → degradation + neighborhood_stability + drift；422 缺/空；带 API key） |
| 平台协整 | `POST /api/platform/cointegration`（Engle-Granger：`y`/`x` + 可选 `zscore_window` → beta/alpha/R²/spread/z-score/半衰期/Durbin-Watson；422 缺/不等长；带 API key） |
| 平台 Kelly | `POST /api/platform/kelly`（binary `win_prob`/`win_size`/`loss_size` 或 `returns` 序列 → full/half/quarter/3-4 Kelly + 期望对数增长 + 可选 `bankroll_units` 破产概率；422 缺；带 API key） |
| 平台波动率 | `POST /api/platform/volatility`（`returns` + 可选 `highs`/`lows` + `lam`/`alpha`/`beta` → EWMA + GARCH(1,1) + Parkinson 序列与最新值；422 缺/短；带 API key） |
| 平台微观结构 | `POST /api/platform/microstructure`（`volumes`/`opens`/`closes` + 可选 `bucket_size`/`window` → VPIN + OFI + Kyle λ；422 缺/不等长；带 API key） |
| 平台执行成本 | `POST /api/platform/execution-cost`（Almgren-Chriss：`total_shares`/`n_slices` + `eta`/`sigma`/`risk_aversion` + 可选 `frontier` → trajectory/expected_cost/risk + 前沿；422 缺；带 API key） |
| 平台 Hawkes | `POST /api/platform/hawkes`（`events` 事件时序 + 可选 `mu`/`kappa`/`beta` → branching_ratio + log_likelihood + stationary；422 缺/空；带 API key） |
| 平台历史压力 | `POST /api/platform/historical-stress`（`positions` {sym:[qty,price]} + 可选 `episodes`/`capital_buffer`/`confidence` → per-episode PnL + worst/best + 95% 失 + 资本充足率；422 缺/空；带 API key） |
| 平台因子风险 | `POST /api/platform/factor-risk`（`weights`/`exposures`/`factor_cov`/`idio_var` → portfolio/factor/idio variance + per-factor 贡献/份额；422 缺/空；带 API key） |
| 平台参数敏感度 | `POST /api/platform/sensitivity`（`records` [{params, metric}] → 一阶/全阶 Sobol + 交互 + 排名；422 缺/空；带 API key） |
| 平台极值理论 | `POST /api/platform/evt`（`losses` + `threshold` + 可选 `confidence_levels` → GPD xi/sigma + 尾部 VaR/CVaR；422 缺/空；带 API key） |
| 平台因果发现 | `POST /api/platform/causal-analysis`（`x`/`y` + `max_lag` + 可选 `z`/`mode` → Granger F/p-value + PCMCI 偏相关滞后 + lead-lag；422 缺/空；带 API key） |
| 平台 HMM 状态 | `POST /api/platform/regime-hmm`（`returns` + 可选 `n_states`/`n_iter`/`tol` → Baum-Welch 参数 + Viterbi 状态序列 + BULL/BEAR/SIDEWAYS 标签；422 缺/空；带 API key） |
| 平台 Copula | `POST /api/platform/copula`（`x`/`y` → Kendall τ + Gumbel/Clayton θ + 上下尾相依；422 缺/空/常数；带 API key） |
| 平台回撤预测 | `POST /api/platform/drawdown-forecast`（`equity` 或 `returns` + `horizon_bars` + 可选 `confidence` → underwater 期 + 恢复时分布 + 期望/分位回撤；422 缺/空；带 API key） |
| 平台流动性 | `POST /api/platform/liquidity-metrics`（`returns` + 可选 `volumes`/`market_returns`/`highs`/`lows` → Amihud/Roll/Pastor-Stambaugh/Corwin-Schultz；422 缺/空；带 API key） |
| 平台动量因子 | `POST /api/platform/momentum-factors`（`price_panel` {asset:[…]} + 可选 `mode`/`lookback`/`holding`/`n_long`/`n_short` → 多空腿 + Jegadeesh-Titman/De-Bondt；422 缺/空；带 API key） |
| 平台组合分解 | `POST /api/platform/portfolio-decomposition`（`returns` + `factor_returns` → 因子 β/α/R²/贡献/残差；422 缺/空/不等长；带 API key） |
| 平台 SPA 检验 | `POST /api/platform/spa-test`（`benchmark_lf` + `model_lfs` + 可选 `B`/`block_length` → Hansen-White SPA p-value + 逐模型 p；422 缺/空；带 API key） |
| 平台执行质量 | `POST /api/platform/execution-quality`（`fills` + 可选 `benchmark_prices`/`post_fill_prices` → 统计 + 滑点分布 + adverse-selection + 评级；422 缺/空；带 API key） |
| 平台分散度 | `POST /api/platform/diversification`（`weights` + `sigmas` + `cov` → effective N + DR + 分散收益 + HHI；422 缺/空；带 API key） |
| 平台期权定价 | `POST /api/platform/options-pricing`（BSM 欧式 call/put price + Δ/Γ/ν/Θ/ρ/vanna/volga；422 缺/非正；带 API key） |
| 平台隐含波动率 | `POST /api/platform/implied-volatility`（`mode=iv`：Newton+bisection 反演 σ；`mode=svi`：Gatheral SVI 5 参原始参数化 NLSQ 拟合；422 缺/越界；带 API key） |
| 平台 Kalman 滤波 | `POST /api/platform/kalman-filter`（线性 Kalman predict/update（Joseph 形式）+ 可选 RTS 平滑；`observations`/`F`/`H`/`Q`/`R`/`x0`/`P0` + `smooth?`/`B`?/`u`?；422 缺/奇异；带 API key） |
| 平台随机过程 | `POST /api/platform/stochastic-processes`（`process` gbm/ou/cir/merton_jd Euler 仿真 + 解析矩；422 缺/非法；带 API key） |
| 平台统计套利 | `POST /api/platform/stat-arb-signals`（距离法 spread + z-score 进出场（LONG/SHORT/FLAT）+ 复用 P223 OU 半衰期；422 缺/不等长/非法阈值；带 API key） |
| 平台鲁棒统计 | `POST /api/platform/robust-statistics`（MAD/winsorize/trimmed mean/Huber IRLS + 可选 Theil-Sen 回归；422 缺/空；带 API key） |
| 平台 Bandit | `POST /api/platform/bandits`（ε-greedy/UCB1/Thompson Beta+Gaussian 仿真 + regret；422 缺/非法算法；带 API key） |
| 平台 LOESS | `POST /api/platform/loess`（Cleveland tricube 局部线性 + 鲁棒 bisquare 迭代；422 缺/不等长/非法带宽；带 API key） |
| 平台订单路由 | `POST /api/platform/smart-order-routing`（多 venue L1 最优价贪婪 + tick 量化 + 拆单 + WAP/费用；422 缺/空/非法 side；带 API key） |
| 平台 Vine Copula | `POST /api/platform/vine-copula`（C-vine/D-vine 逐对 copula（Gaussian/Gumbel/Clayton，复用 P235）+ 对数似然 + AIC/BIC；422 缺/空/常数列；带 API key） |
| 平台谱分析 | `POST /api/platform/spectral-analysis`（DFT periodogram + 主频/谱熵/频段能量；422 缺/非法；带 API key） |
| 平台周期检测 | `POST /api/platform/cycle-detection`（自相关周期候选 + Ljung-Box 近似 + seasonal_strength；422 缺/非法周期；带 API key） |
| 平台变点检测 | `POST /api/platform/change-point`（均值/方差漂移 + best-first binary segmentation；422 缺/非法；带 API key） |
| 平台熵复杂度 | `POST /api/platform/entropy-complexity`（Shannon/sample/permutation entropy + Hurst R/S；422 缺/非法；带 API key） |
| 平台滚动特征 | `POST /api/platform/rolling-features`（rolling mean/std/zscore/skew/kurtosis + EWMA + rolling beta；422 缺/非法；带 API key） |
| 平台因子 IC | `POST /api/platform/factor-ic`（Pearson/Spearman rank IC + quantile buckets + ICIR；422 缺/不等长；带 API key） |
| 平台特征正交化 | `POST /api/platform/feature-orthogonalization`（Gram-Schmidt、residualize、correlation prune、VIF；422 缺/非法 panel；带 API key） |
| 平台信号组合 | `POST /api/platform/signal-combination`（zscore/rank/raw 合成 + 显式/等权 weights；模块函数支持 risk-budget weights；422 缺/非法 method/weights；带 API key） |
| 平台回测诊断 | `POST /api/platform/backtest-diagnostics`（expectancy/profit-factor/payoff/streak/bootstrap CI；无亏损比率以 `"Infinity"` JSON-safe 输出；422 缺/非法；带 API key） |
| 平台数据质量 | `POST /api/platform/data-quality`（timestamp gap/duplicate/order + stale/jump/non-positive close + OHLC 一致性；422 缺/非法 schema；带 API key） |
| 组合配置 | `GET /api/portfolio/config`（组合配置列表）、`PUT /api/portfolio/config/{name}`（保存/更新组合配置；400 name 不匹配、422 校验/缺字段；带 API key） |
| 组合归因 | `GET /api/portfolio/attribution?name=`（按标的 FIFO realized + unrealized PnL 与贡献；未知组合 404；带 API key） |
| 组合 kill-switch | `GET/POST /api/portfolio/kill-switch`、`POST /api/portfolio/kill-switch/disable`（模块级熔断；arm 写审计 `PORTFOLIO_KILL_SWITCH`；arm 后 `PortfolioRunner.rebalance` 跳过） |
| 策略版本 | `GET /api/strategy/versions`（参数版本快照列表）、`POST /api/strategy/versions/{id}/rollback`（回滚并写审计 `STRATEGY_ROLLBACK`；未知 404） |
| 策略 | `GET/PUT /api/strategy`（含 `trading_session_mode`、`fee_rate_*`、`llm_*`），`GET /api/status`，`GET /api/status/history` |
| 控制 | `POST /api/control/{start,stop,pause,resume,kill-switch,disable-kill-switch}`（全部写审计） |
| 交易 | `GET /api/orders`（`refresh=true` 强制同步今日订单），`POST /api/orders/{id}/cancel`（写审计），`GET /api/account`（批量 quote） |
| 交易笔记 | `GET /api/trade-notes`（`?symbol=`）、`GET /api/trade-notes/analytics`（聚合），`GET/PUT/DELETE /api/trade-notes/{order_id}`（每单一笔记 note/tags/rating 1-5；PUT/DELETE 写审计 `TRADE_NOTE_*`；订单不存在 404） |
| 持仓浮盈 | `GET /api/positions/pnl`（`tracked_entries` 成本 × 实时行情 → 未实现 P&L；只读） |
| 风险历史 | `GET /api/risk/history`（`runtime_state_snapshots` 时序快照：daily_pnl/连续亏损/暂停/熔断；只读） |
| 行情 K 线 | `GET /api/broker/candles`（`BrokerGateway.get_candlesticks` → `{bars, csv_text}`，回测用；422 非法 period / 503 券商不可用） |
| 交易时段 | `GET /api/calendar/session`（`session_status` → `rth`/`pre`/`post`/`lunch`/`closed` + 本地时间 + 下次开盘；只读） |
| 通知中心 | `GET /api/notifications`（已发送通知分发日志，`notifications` 表；`MultiChannelNotifier` sink 落库；只读）；`GET /api/notifications/export?format=csv|json`（同过滤条件无分页导出）；`POST /api/notifications/{id}/retry`（失败通知使用当前凭证重发并更新原记录） |
| LLM 详情 | `GET /api/llm-interactions/{id}`（单条交互完整 prompt/响应/解析/上下文；只读） |
| 笔记分析 | `GET /api/trade-notes/analytics`（笔记聚合：评分分布/热门标签；只读） |
| 定时报告 | `POST /api/reports/schedule/run`（立即推送日报；写审计）；后台 cron 按 `StrategyConfig.report_schedule_*` 周期推送 |
| 告警规则 | `GET/POST /api/alert-rules`、`PUT/DELETE /api/alert-rules/{id}`、`POST /api/alert-rules/evaluate`（`price_above`/`price_below`/`daily_loss`；后台每 60s 评估；触发经 `MultiChannelNotifier`；只读评估不发单） |
| 策略预设 | `GET/POST /api/strategy-presets`、`GET/DELETE /{id}`、`POST /{id}/apply`（应用到当前策略配置，写审计 `STRATEGY_PRESET_APPLY`） |
| 事件 | `GET /api/events`（`source=trade\|audit\|llm\|risk\|all` 跨四表 union：trade_events + audit_logs + llm_interactions + risk_events；`event_type` 重复参数多选），`GET /api/events/export`（仅 `trade_events`） |
| 凭证 | `GET/PUT /api/credentials`（含 `notification_channels`，写审计），`POST /api/credentials/test`（全渠道冒烟测试），`POST /api/credentials/notification-channels/test`（单渠道连通性诊断） |
| LLM | `POST /api/strategy/llm-interval/{preview,analyze}`，`GET …/status`，`PUT …/enable\|disable` |
| 回测 | `POST /api/backtest/run`、`POST /api/backtest/sweep`（即时网格参数扫描，返回排名表+热力图）、`POST /api/backtest/walk-forward`（滚动窗口样本外稳定性评估）、`POST /api/backtest/stress`（What-If 蒙特卡洛压力分布）、`POST /api/backtest/export`（多 section CSV 导出结果） |
| 实验 | `GET /api/experiments`（实验名称列表），`GET /api/experiments/{name}/summary`（变体摘要） |
| 指标 | `GET /api/indicators?symbol=`（实时技术指标快照：ATR/RSI/MACD/布林带/成交量/情绪/多时间框架；只读，不触发 LLM） |
| 性能 | `GET /api/performance/stats?experiment=`，`GET /api/performance/compare?experiment=`，`GET /api/performance/recommendations?experiment=`（A/B 统计；只读） |
| 实时 | `WS /ws` |
| 健康 | `GET /api/health` |

## 约束 & 注意事项

### Python 版本

**必须 3.11+**。`tests/test_ws.py` 在 3.9 下会因 `asyncio.Lock()` 离开 running loop 而失败。

### 测试约定（容易踩坑）

1. **每个测试文件用独立的 sqlite 文件**（`data/test_xxx.db` 或 `tmp_path`），在 `from app...` **之前**设置 `AUTO_TRADE_DATABASE_URL`。
2. **`conftest.py` 已设置**进程级 `AUTO_TRADE_CREDENTIAL_KEY_PATH`（临时 PEM），避免与开发机 `data/credential_private_key.pem`（可能带 KEK 加密）冲突。
3. **不要用 `os.environ.pop`**——改用 `monkeypatch.delenv`。
4. **`setup_class` 先 `drop_all` 再 `create_all`**；旧 DB 文件缺列会导致测试失败。
5. **`database.engine` 在 import 时绑定 `settings.database_url`**；测试中须在 import 前设 env，或 `monkeypatch` 替换 `database.engine`。

### 数据库迁移

- 运行时通过 `init_db()` 顺序执行：`_ensure_order_execution_columns`、`_ensure_order_raw_response_column`、`_ensure_strategy_config_llm_columns`、`_ensure_strategy_config_trade_safety_columns`（P4：`fee_rate_us`、`fee_rate_hk`、`min_repricing_pct`、`llm_action_cooldown_seconds`）、`_ensure_strategy_config_session_columns`（P5+：`trading_session_mode`）、`_ensure_runtime_state_daily_pnl_date_column`、`_ensure_audit_log_table`（P5+）、`_ensure_credential_config_notification_channels_column`（P5+，回填默认 `[{"type":"serverchan","severity_floor":"INFO"}]`）、`_ensure_tracked_entries_table`、`_ensure_trade_notes_table`（P43+，交易笔记：每单一笔记 `note`/`tags_json`/`rating 1-5`，唯一 `order_id`）、`_ensure_strategy_config_report_schedule_columns`（P46+：`report_schedule_enabled`/`report_schedule_interval_hours`/`report_schedule_symbol`）、`_ensure_backtest_runs_table`（P50+）、`_ensure_alert_rules_table`（P51+：`rule_type`/`threshold`/`severity`/`cooldown_seconds`/`last_fired_at`）、`_ensure_strategy_presets_table`（P52+：name + params_json）、`_ensure_notifications_table`（P58+：title/content/severity/success/error）。
- **`alembic/` 不用于生产**；加列须同步新增 `_ensure_*`。
- 首次补 `daily_pnl_date` 时会把 NULL 行的 `daily_pnl`/`consecutive_losses` 置 0（一次性）。

### 配置

- 业务 settings：`AUTO_TRADE_` 前缀（`app/config.py`）。
- 长桥凭据：`LONGPORT_*` 优先，兼容 `LONGBRIDGE_*`（`merge_longbridge_credentials`）。
- `CREDENTIAL_MASTER_KEY`：加密 DB 内凭据及 PEM；**轮换后旧密文不可解**。
- `AUTO_TRADE_CREDENTIAL_KEY_PATH`：RSA 私钥路径，默认 `data/credential_private_key.pem`。
- `DEEPSEEK_API_KEY`：LLM 顾问（无此前缀，见 config 的 `validation_alias`）。
- `DEEPSEEK_MODEL` / `DEEPSEEK_REASONING_EFFORT` / `DEEPSEEK_THINKING_TYPE`：默认 `deepseek-v4-pro` + thinking `max`（`enabled`）。
- `AUTO_TRADE_BROKER_RETRY_MAX`（默认 3，订单全量）、`AUTO_TRADE_BROKER_QUOTE_RETRY_MAX`（默认 1，行情）、`AUTO_TRADE_BROKER_RETRY_BASE_MS`（默认 1000）：`BrokerGateway._call_with_retry` 分档退避；`max_retries=0` 表示只调 1 次，无 sleep。
- `AUTO_TRADE_AUDIT_REQUEST_SUMMARY_LIMIT`（默认 2048）：审计 `request_summary` 截断字节数；超出会附加 `...truncated`。
- **API 鉴权行为**：`require_api_key()` 读取 `X-API-Key`；`AUTO_TRADE_API_KEY` 为空时仅 `dev/test` 放行，`prod` 返回 401。Docker/nginx 与 Vite 代理在服务端注入 header，前端 SPA 不持有该密钥。

### 平台模式（P149+P150）

- `AUTO_TRADE_PLATFORM_MODE=false`（默认）使用旧 `AppRunner`，新平台层仅暴露 `/api/platform/strategies` 等只读端点。
- `AUTO_TRADE_PLATFORM_MODE=true` 在 lifespan 中额外初始化 `PlatformRunner` 并挂到 `app.state.platform_runner`；`PlatformRunner` 通过统一事件流驱动策略插件。
- 新增 `app/platform/` 目录：`events.py`、`sdk/`、`context.py`、`registry.py`、`bus.py`、`store.py`、`runner.py`、`replay.py`、`simbroker.py`、`api.py`。
- 现有区间策略已迁移为 `app/strategies/interval_strategy.py`，作为首个策略插件。
- Phase 1 已知简化：`SimBroker` 仅支持 LIMIT 单全部成交；`PlatformRunner` live 模式仅预留 `live_order_handler` 回调，未与 `TradeExecutionService` 完整接线。
- **组合交易（P151+P152+P156）**：`PlatformRunner` 改用 `symbols: list[str]`（保留 `symbol` 参数与只读 `.symbol` 属性兼容旧调用），backtest/paper 模式注入 `PaperBroker`（partial fill/滑点/费用/撤改单，替代 `SimBroker`）。`PortfolioConfig`（模型 + dataclass）描述目标权重/风险预算；`PortfolioAllocator` 生成再平衡 `OrderIntent`；`PortfolioService`/`portfolio_api.py` 提供 `GET /api/portfolio/config`、`PUT /api/portfolio/config/{name}`（带 `require_api_key`、400 name 不匹配、422 校验/缺字段）。`FillEvent` 扩展 `slippage`/`commission`/`partial`，`OrderEvent` 扩展 `reason`。`PortfolioRiskController` 计算 gross/net 敞口与回撤（`MAX_GROSS/NET_EXPOSURE_BREACH` CRITICAL、`DRAWDOWN_BREACH` WARNING），`RiskEngine` 订阅 fill、每 bar 经 `runner._emit` 发出 `RiskEvent`。`PaperOrder` 表（`_ensure_paper_orders_table`）持久化留待后续接线。
- **平台闭环（P153–P162）**：`PaperBroker` 持久化订单到 `paper_orders`（`from_db` 可重载未结订单），`OrderIntent` 新增 `stop_price`/`trailing_offset`/`linked_order_id`（STOP/TRAILING 撮合 + OCO 联动撤单）。`PortfolioRiskController` 增集中度（`CONCENTRATION_BREACH`）与滚动相关性（`CORRELATION_BREACH`）。`POST /api/platform/backtest`（`PlatformBacktestService` 在 K 线上跑策略 → equity/fills/positions）、`GET /api/platform/snapshot`（运行中 runner 持仓/未结单）、`GET /api/platform/events`（分页）+ `POST /api/platform/replay`（确定性回放）。`GET /api/portfolio/attribution`（FIFO realized + unrealized）。`StrategyParamVersion` 表 + `GET /api/strategy/versions` + `POST /{id}/rollback`（写审计 `STRATEGY_ROLLBACK`，回滚本身也留版本）。`PortfolioRunner`（`portfolio_runner.py`）驱动 `PortfolioAllocator` 再平衡经 `PlatformRunner.submit_intent`，带模块级 kill-switch（`arm/disarm/is_armed`，`POST /api/portfolio/kill-switch{,/disable}` 写审计 `PORTFOLIO_KILL_SWITCH`）与 CRITICAL 风控 gate（rebalance 前查 `risk_engine.controller.check`，CRITICAL 即跳过）。
- **研究/执行深度（P163–P172，参考 Nautilus/Lean/Backtrader/pyfolio）**：`PerformanceAnalytics`（Sharpe/Sortino/maxDD/Calmar/win-rate/profit-factor，并入回测 + `POST /api/platform/analyze`）。`Portfolio`（`portfolio.py`）为 cash/positions/NAV/realized 单一真相源，`_BacktestCollector` 与 `RiskEngine`（可注入）从其读取。`Sizer`（`sizers.py`：FixedFractional/FullEquity/ATR + 注册表 + `intent_from_signal`）。`IndicatorService`（`indicators.py`：SMA/EMA/RSI/ATR 滚动缓冲 + 缓存，经 `StrategyContext.indicators` 取用，runner 自动喂数据）。`Universe`（`universe.py`：Static/TopNByVolume，runner 路由前 gating）。`ExecutionClient` Protocol + `LiveExecutionClient`（`execution.py`，runner 解耦具体 broker）。`DataCatalog` + `resample_bars`（`data_catalog.py`，时间桶 1m→Nm）+ `GET /api/platform/bars`。`OptimizerService`（`optimizer_service.py`：grid + walk-forward IS/OOS）+ `POST /api/platform/optimize`。`analyzers.py`：TradeAnalyzer/DrawDownAnalyzer/ReturnsAnalyzer（per-trade + underwater + 分布）。`FillModel`（`fill_model.py`：SlippageModel/CommissionModel Protocol + Fixed/VolumeShare/Fractional/PerShare，PaperBrokerConfig 可选注入替代固定系数）。
- **机构级编排（P173–P182，参考 Nautilus OMS/Position、Lean 构造/调度/CashBook、事件溯源 CQRS、vectorbt/QuantStats）**：`OrderManagementSystem`（`oms.py`，订阅 order_intent/order/fill，best-effort 充实 side/qty，加权均价 fill，按状态查询）。`PositionEngine`（`position_engine.py`，typed FLAT/LONG/SHORT，净额 + 多空翻转 + 每仓 realized）。`BenchmarkAnalytics`（`benchmark.py`，alpha/beta/correlation/tracking-error/information-ratio/up-down capture，`/analyze` 接 `benchmark_equity`）。`PortfolioConstructionModel`（`construction.py`，EqualWeight/RiskParity 反波动加权 + `weights_to_intents`）。`WarmupProvider` + runner.warmup（`warmup.py`，Lean SetWarmup 语义：预热期 `_execute_intent` 短路，只暖指标/策略）。`Scheduler`（`scheduler.py`，every-N-bars + daily-at-time，runner 自动 tick）。`Transaction` 表 + `TransactionService`/`TransactionLogger`（`transaction_service.py`，per-fill 落账 + `GET /api/platform/transactions`）。`MonteCarloAnalyzer`（`montecarlo.py`，seeded bootstrap 重采样 → 最终 PnL 分位/破产概率/分位路径 + `POST /api/platform/montecarlo`）。`Projection` 框架（`projections.py`：NavProjection/DailyReturnsProjection + `ProjectionEngine` 订阅 bus 构建读模型，CQRS 快照）。`CashBook`（`cashbook.py`：按币种记账 + FX 汇率聚合基币 NAV，`on_fill` 按标的币种路由，`currency_for` `.HK`→HKD 其余 USD）。
- **执行算法与研究层（P183–P192，参考 Nautilus ExecutionAlgorithm、Alpha101/alphalens、Brinson、Optuna/Hyperband、Nautilus TradingSession/MarginModel/LatencyModel、pyfolio/QuantStats）**：`ExecutionAlgorithm`（`execution_algorithms.py`：TWAP/VWAP/Iceberg 把父单拆为子切片 + 注册表）。`Factor` 库（`factors.py`：momentum/volatility/meanrev 时序因子 + 横截面 `information_coefficient`）。`StrategyCombinator`（`strategy_combinator.py`：N 策略加权 alpha 合流，自身实现 Strategy）。`brinson_attribution`（`brinson.py`：BHB allocation/selection/interaction 分解）。`PlatformBacktestRun` 表 + `BacktestRunService`（`backtest_run_service.py`：命名保存回测结果 + list/get/compare，`POST/GET /api/platform/backtest/runs*`）。`SmartOptimizer`（`smart_optimizer.py`：准随机采样 + successive-halving 中位剪枝，比 grid 高效）。`MarketSessionFilter`（`session_filter.py`：pre/rth/post/closed，runner 路由前按 `allowed_sessions` gate）。`MarginModel` + `LeverageGuard`（`margin.py`：固定保证金率 + 杠杆上限 gate + 可用容量）。`TearsheetBuilder`/`TearsheetExporter`（`tearsheet.py`：聚合 summary/equity/returns/drawdown/trades + CSV/JSON 导出，`POST /api/platform/tearsheet`）。`LatencyModel`（`latency.py`：FixedLatencyModel；PaperBroker 经 `submit_delay` 把订单置 QUEUED、按 bar 倒计晋升 SUBMITTED，`fill_delay` 持有匹配 fill 延迟 emit）。
- **纵深补齐（P193–P202，参考 Nautilus MessageBus/OrderBook/ContinuousContract/StrategyPool/CostModel、Lean MessagingBus/ContinuousContract/FillMatrix、alphalens/Qlib、RiskMetrics、López de Prado PBO/Deflated Sharpe、Brinson-Ibbotson/Frongello）**：`Transport` Protocol + `DistributedEventBus`（`distributed_bus.py`：Redis/InMemory/Null 适配，本地分发优先、transport best-effort，`subscribe_remote` 远端回放不回环）。`OrderBook`（`order_book.py`：bid/ask 价格层 FIFO 队列 + MARKET/LIMIT 跨层 partial + 撮合后余量回挂）。`ContinuousContractBuilder`/`build_continuous`（`continuous_contract.py`：按换月点缝合，RATIO/BACKWARD/NONE 三法调整，最近段不调整、历史累计调整）。`FactorResearchService` + `factor_snapshots`/`factor_ic_series` 表（`factor_research_service.py`：per-(date,symbol,factor) 快照 + 截面排名 + IC 时序聚合，`GET/POST /api/platform/factors/snapshots`、`GET /api/platform/factors/ic`）。`StrategyIsolationManager`（`strategy_isolation.py`：每策略独立 `Portfolio` + 资本预留校验 + 按 order 路由 fill + 聚合 NAV 与归因）。`ExponentialDecay`/`RollingWindowDecay`/`reweight_combinator_weights`（`signal_decay.py`：半衰期权重 + 在线窗口 + 静态×衰减再平衡，确定性无 RNG）。`TcaAnalyzer`（`tca.py`：按 symbol/side/source/time-bucket 归因已实现滑点+佣金，签约滑点正=不利，参考价可注入，`GET /api/platform/tca`）。`ScenarioLibrary`（`stress_scenarios.py`：equity_crash/volatility_spike/correlation_breakdown/liquidity_discount 确定性场景 + 按 beta 缩放 + summary 序列化）。`probability_of_backtest_overfitting`/`deflated_sharpe_ratio`（`overfitting.py`：PBO 用 CSCV 组合对称交叉验证枚举、DSR 用闭式 erf CDF 多试验/非正态校正，无 RNG）。`brinson_multi_period`/`link_arithmetic`/`link_geometric`（`brinson_multiperiod.py`：单期 Brinson 序列 + 算术 BHB 链接 + Frongello 几何链接 + residual 对账）。
- **风险科学 + 投资组合优化（P203–P212，参考 PyPortfolioOpt / Jorion / López de Prado / Burke / Kestner / Hill / McCulloch）**：`covariance.py`（Ledoit-Wolf 2004 常相关目标收缩）+ `mean_variance.py`（Markowitz min-var/max-Sharpe + efficient_frontier + `MeanVarianceModel`）+ `black_litterman.py`（Black-Litterman 1991 反优化 prior + 视图 posterior + `BlackLittermanModel`）+ `hrp.py`（López de Prado HRP 单链 quasi-diagonalize + 递归 bisection + `HRPModel`）+ `risk_metrics.py`（历史+参数 VaR/CVaR，Acklam 高精度正态逆 CDF，多资产 `portfolio_var`）+ `drawdown_analysis.py`（回撤事件 + duration/recovery_time + rolling Calmar + underwater + acceleration）+ `risk_ratios.py`（Sharpe/Sortino/Information/Treynor/Modigliani M²/Omega + 滚动 Sharpe）+ `pain_metrics.py`（Pain Index / Ulcer Index / MAR / Kestner ratio）+ `fat_tail.py`（excess kurtosis / skewness / Hill 估计器 / tail ratio / 稳定分布 4 参数 fit）。**新增端点：** `POST /api/platform/risk-metrics`（统一入口：VaR/CVaR + drawdown + pain + tail + ratios）+ `POST /api/platform/portfolio-optimize`（4 种 method：`min_variance` / `max_sharpe` / `hrp` / `black_litterman`）。所有模块均**零新依赖**、可选注入、确定性；与 `PlatformRunner` / `BacktestService` 解耦，可独立离线使用。
- **风险研究 + 执行深度 II（P213–P222，参考 Nautilus MarketRegimeModel、López de Prado CPCV、Sharpe 1992 Style Analysis、PyPortfolioOpt turnover、Maillard-Roncalli/Spinu ERC、vectorbt MFE/MAE、Perold Implementation Shortfall、pyfolio returns calendar、FRB CCAR stress、Optuna parameter stability）**：`regime.py`（BULL/BEAR/SIDEWAYS：SMA 交叉斜率 + Wilder ADX + 实现波动 + slope/vol fallback；`RegimeModel` 流式喂 BarEvent，状态变化发 `RegimeEvent`，`rolling_regime`/`regime_report`）+ `cpcv.py`（组合交叉验证：`C(N,k)` 组合枚举 + purge 对称 + embargo 后置 + `cpcv_oos_paths` 贪心不交集 + `cpcv_pbo` 复用 `overfitting`）+ `style_analysis.py`（Sharpe 1992：纯 Python Lawson-Hanson NNLS active-set + 等式约束 simplex KKT，`none`/`sum_le_one`/`sum_eq_one` 三模式 + R²/tracking-error/annualized）+ `turnover_optimization.py`（`wᵀΣw − λμᵀw + γ·L1turnover`，projected subgradient + Duchi simplex 投影 + 可选 `delta_cap` L1 上限 proximal 投影，`TurnoverAwareModel`）+ `risk_budgeting.py`（Maillard-Roncalli/Spinu 凸风险预算：Newton 迭代 log-barrier 目标 `f(y)=½yᵀΣy − Σb·ln(y)`，Hessian 对角障碍项恒 PD 即使 Σ 奇异，`risk_contributions`/`relative_risk_contributions`，`RiskBudgetingModel`）+ `trade_excursion.py`（per-trade MFE/MAE + holding_bars + entry/exit timing rank + `trades_from_fills` FIFO 配对 + 百分位汇总 + mfe_mae_ratio）+ `shortfall.py`（Perold 1988 Implementation Shortfall：realized/opportunity/timing/fees + VWAP/arrival/close benchmark + bps + participation_rate + `shortfall_from_tca` 复用 `TcaFill` + `ShortfallAnalyzer` 读 transactions）+ `returns_analysis.py`（monthly/yearly/weekday/streaks + 合成日历 epoch 2000-01-03 Mon + `monthly_returns_table` 透视）+ `stress_report.py`（聚合 `stress_scenarios`+`risk_metrics`+`drawdown_analysis`：per-scenario PnL/VaR + worst-scenario + 资本充足率 + `StressReportBuilder`）+ `stability_analysis.py`（walk-forward IS/OOS 退化比 + IS-vs-OOS Spearman rank-corr + 邻域稳定性 Hamming-1 + 最优参数漂移 numeric/categorical）。**新增端点：** `POST /api/platform/regime` + `POST /api/platform/cpcv` + `POST /api/platform/style-analysis` + `POST /api/platform/trade-excursion` + `POST /api/platform/shortfall` + `POST /api/platform/returns-calendar` + `POST /api/platform/stress-report` + `POST /api/platform/stability`，并扩展 `POST /api/platform/portfolio-optimize` 新增 `turnover`/`risk_budgeting` method（含 `risk_contributions`/`turnover` 诊断字段）。全部**零新依赖**、确定性、纯 Python（numpy 仍未引入），可选注入、默认路径零行为变更。
- **配对/微观/执行/风险研究 III（P223–P232，参考 Engle-Granger 协整、Thorp/Kelly、RiskMetrics/Bollerslev GARCH、Easley López de Prado VPIN、Almgren-Chriss、Hawkes、FRB CCAR 历史场景、Barra 因子风险、Saltelli fANOVA、McNeil-Frey EVT/GPD）**：`cointegration.py`（Engle-Granger 2-step OLS 对冲比 + 残差 spread + Durbin-Watson 自相关 + OU 半衰期 `ln2/κ` + 滚动/全样本 z-score，`CointegrationResult`）+ `kelly.py`（binary Kelly `f*=(pb−q)/b` + 连续 Kelly `μ/σ²` long-only clamp + fractional Kelly（full/half/quarter/3-4） + 期望对数增长 `p·ln(1+fb)+q·ln(1−fa)` + Thorp/Vince 破产概率 `((1−edge)/(1+edge))^depth`）+ `volatility_models.py`（EWMA RiskMetrics `λσ²ₜ₋₁+(1−λ)r²` + GARCH(1,1) `ω+αr²+βσ²` 长方差驱动 + Parkinson `1/(4ln2)·ln(H/L)²` 高低价，`VolatilityReport`）+ `microstructure.py`（VPIN 等量分桶 + close/open bulk-classification + 滚动失衡比 + OFI 签名量代理 + Kyle λ 价格冲击 OLS）+ `execution_cost.py`（Almgren-Chriss 风险厌恶 sinh 轨迹 + 临时冲击 `η·Σv²` + 时间风险 `σ²·Σx²` + λ 扫描有效前沿，`AlmgrenChrissResult`）+ `hawkes.py`（指数自激发强度 `μ+Σκe^{−βΔ}` + branching ratio `κ/β` + Lewis 2011 递归对数似然 + 强度路径，`HawkesFit` 矩估计默认）+ `historical_scenarios.py`（历史 episode 库 + 默认 5 场景（COVID/rate shock/carry unwind/vol-mageddon/bank stress） + `apply_scenario` + worst/best/95% 失/资本充足率，`HistoricalStressReport`）+ `factor_risk.py`（Barra 式 `σ²_p=wᵀ(BFBᵀ+D)w` + Menchero 边际 per-factor 贡献 `(Bᵀw)·(FBᵀw)` + 特质 `wᵀDw` + factor_share，`FactorRiskDecomposition`）+ `sensitivity.py`（Saltelli 一阶 `Var_{xᵢ}[E_{x₋ᵢ}[Y]]/Var[Y]` + 全阶 `1−Var_{x₋ᵢ}[E_{xᵢ}[Y]]/Var[Y]` + 交互 `S_T−S` + 按全阶排名，`SensitivityReport`）+ `extreme_value.py`（POT 超阈值 + GPD MoM 拟合 `ξ=½(1−m²/var) σ=½m(1+m²/var)` + 尾部 VaR/CVaR 外推 `u+(σ/ξ)[p^{−ξ}−1]`，`EvtResult`）。**新增端点：** `POST /api/platform/cointegration` + `POST /api/platform/kelly` + `POST /api/platform/volatility` + `POST /api/platform/microstructure` + `POST /api/platform/execution-cost` + `POST /api/platform/hawkes` + `POST /api/platform/historical-stress` + `POST /api/platform/factor-risk` + `POST /api/platform/sensitivity` + `POST /api/platform/evt`。全部**零新依赖**、确定性、纯 Python（numpy 仍未引入），可选注入、默认路径零行为变更。
- **因果/诊断/风险研究 IV（P233–P242，参考 Granger/Runge-PCMCI、Hamilton-Rabiner HMM、Sklar-Nelsen/Embrechts copula、Burke-Johansen-Magdon-Ismail drawdown、Amihud-Roll-Pastor-Corwin-Schultz liquidity、Jegadeesh-Titman/De Bondt-Thaler/Carhart/Moskowitz-TSMOM、Fama-French/Barra/Grinold-Kahn、Hansen-White SPA/Politis-Romano、FIX/Kissell execution quality、Choueifady-Coignard/Bouchard-Potters diversification）**：`causal_analysis.py`（Granger 1969 F 统计 `((RSS_r−RSS_u)/L)/(RSS_u/(n−2L−1))` + 自实现 continued-fraction `betai` 算 p 值（无 scipy）+ PCMCI Runge 2019 偏相关滞后（递归偏相关经小相关矩阵高斯消元）+ `lead_lag_summary`，`GrangerResult`/`LeadLagResult`）+ `regime_hmm.py`（Hamilton 1989 高斯发射 HMM：Baum-Welch 矩估计 E/M 步 + log-scale forward/backward（scaling-factor 防下溢）+ Viterbi log-space 最优路径 + 分位数 bin 确定性初始化（无 RNG）+ `regime_label` BULL/BEAR/SIDEWAYS，`HmmFitResult`）+ `copula.py`（Sklar 1959：`empirical_copula` rank 伪观测 + Kendall τ 一致对 O(n²) 计数 + Gumbel `θ=1/(1−τ)` + Clayton `θ=2τ/(1−τ)` MoM 拟合 + 闭式尾相依 `λ_U=1−2^{1/θ}`/`λ_L=2^{−1/θ}`，`CopulaResult`）+ `drawdown_forecast.py`（`underwater_periods` 峰-恢复段解析 + `recovery_time_distribution` 完成恢复经验分布 + Brownian 反射原理期望最大 `E[M_h]=σ√(2h/π)` + Acklam 逆正态分位回撤 + `DrawdownForecastResult`）+ `liquidity_metrics.py`（Amihud 2002 `|r|/dollar_vol` + Roll 1984 有效价差 `2√(−cov(r_t,r_{t-1}))` + Pastor-Stambaugh 2003 流动性 β（市场收益平方项系数）+ Corwin-Schultz 2012 高低价 2-bar 价差估计，纯 Python 正规方程 OLS，`LiquidityResult`）+ `momentum_factors.py`（Jegadeesh-Titman 1993 横截面排名多空 + De Bondt-Thaler 1985 反转（符号翻转）+ Carhart 1997 + Moskowitz-Ooi-Pedersen 2012 TSMOM 时序动量 + 持有期前瞻收益聚合，`MomentumFactorResult`）+ `portfolio_decomposition.py`（`returns_to_factors` OLS β/α/R²/残差 + 收益贡献对账（含 `reconciliation_error`）+ `variance_decomposition` 系统性/特质拆分 + per-factor 边际贡献，`FactorExposureResult`/`VarianceDecomposition`）+ `spa_test.py`（Hansen-White 2005 superior predictive ability：`d_k=mean(bench_lf)−mean(model_k_lf)` + **确定性** stationary block bootstrap（固定循环偏移块模式，无 RNG——no-numpy/no-random 约束下的诚实折中，已在 docstring 明示）+ 一致 p 值（Hansen 2005 Eq.7）+ 逐模型 p，`SpaResult`）+ `execution_quality.py`（fill 统计：fill_ratio/participation/VWAP/滑点 mean/median/p95 + `price_reversion` adverse-selection（后 fill 价格均值相对 fill 偏离）+ A/B/C/D 评级（|滑点| bps：<2 A、<5 B、<10 C、else D + fill_ratio 加成）+ `ExecutionScorecard`）+ `diversification.py`（Choueifaty-Coignard 2008 DR=`(w'σ)/√(w'Σw)` + effective N=DR² + 分散收益 `1−σ_p/σ_wavg` + Lorenz 累积份额曲线 + HHI 集中度 `Σw_i²`，`DiversificationResult`）。**新增端点：** `POST /api/platform/causal-analysis` + `POST /api/platform/regime-hmm` + `POST /api/platform/copula` + `POST /api/platform/drawdown-forecast` + `POST /api/platform/liquidity-metrics` + `POST /api/platform/momentum-factors` + `POST /api/platform/portfolio-decomposition` + `POST /api/platform/spa-test` + `POST /api/platform/execution-quality` + `POST /api/platform/diversification`。全部**零新依赖**、确定性、纯 Python（numpy 仍未引入；SPA bootstrap 以固定块模式替代随机），可选注入、默认路径零行为变更。

### 交易执行（TradeExecutionService）

- **加权入场成本**保存在内存 + `tracked_entries` 表；`AppRunner` 启动时 `load_tracked_entries`，成交后 `persist_entry`；平仓 PnL 优先 tracked avg，不用 broker `avg_price`。
- HK 限价按港交所阶梯 tick 量化（`.HK`）；US 仍 0.01。
- 实盘下单后走 **pending 异步对账**（`_track_pending_order` + `_reconcile_pending_order`）；已删除阻塞式 `_wait_for_order_completion`。
- `_order_status_timeout_seconds` 默认 30；**`= 0` 表示禁用** reconcile 超时。超时后 pause、清 pending、恢复 engine snapshot。
- `execute()` 入口会再次 `risk.check()`，并拒绝已有 pending 时的并发下单。
- 券商已提交但 **DB 写入失败** → `OrderPersistenceError` 路径：尝试撤单、pause、回滚 snapshot。
- `engine.sync_state()` **不会**清空 `last_trigger_at`（保留冷却）。
- `StrategyConfig` 新增四项：`fee_rate_us` / `fee_rate_hk`（实盘普通平仓双边费用估算）、`min_repricing_pct`（LLM cancel-replace 最小改价阈值）、`llm_action_cooldown_seconds`（LLM 同方向发单冷却）。
- `_profit_guard_for_exit` 在原 `min_profit_amount` 校验上叠加 round-trip 费用：`expected_profit - estimated_fees < required_profit` 即 `ORDER_SKIPPED` 且 `payload.skip_category = "FEE"`；`allow_loss_exit=True` 完全绕过费用门槛。
- LLM `execute_llm_order_decision` 在调用 `cancel_pending_order` 之前执行改价（`REPRICING`）与冷却（`COOLDOWN`）gate；`CANCEL_PENDING` 不受 gate 影响。`_last_llm_action_at[(symbol, broker_side)]` 仅在 FILLED/SUBMITTED/PARTIAL_FILLED 后更新。
- `record_order_skipped` payload 现已稳定带 `skip_category` ∈ `{FEE, REPRICING, COOLDOWN, RISK, PENDING, POSITION, SESSION}`；前端 `skipCategoryLabel` 是唯一渲染入口。
- **交易时段守卫（P5+）**：`StrategyConfig.trading_session_mode ∈ {ANY, RTH_ONLY}`，默认 `ANY` 零行为变更。`RTH_ONLY` 时双层 gate：
  - 层 A（`AppRunner._check_trading_session`）：`execute_llm_order_decision` 在 `_precheck_llm_action` 之后、任何 `cancel_pending_order` 之前；非 RTH 直接 skip + `TRADING_SESSION_BLOCKED` 审计 + `SESSION` skip 事件。
  - 层 B（`TradeExecutionService.execute`）：在 `risk.check()` 之前；非 RTH skip + `skip_category="SESSION"`。
  - `CANCEL_PENDING` 不受时段守卫限制（允许非 RTH 清理挂单）。
  - 仅周末 + 常规 RTH 时段，**不含节假日历**，UI 文案明示。
- **操作审计（P5+）**：`AuditLogger.record(...)` 写 `audit_logs`，`actor_hash = sha256(X-API-Key)[:16]`（无 header → `anonymous`），`source_ip` 取 `X-Forwarded-For` 第一段或 `client.host`。9 个端点接入：`POST /api/control/{start,stop,pause,resume,kill-switch,disable-kill-switch}`、`PUT /api/strategy`（diff）、`PUT /api/credentials`（脱敏 diff）、`POST /api/orders/{id}/cancel`。失败仅 log warning，不抛。
- **多渠道通知（P5+）**：`MultiChannelNotifier.from_credential_config(cred)` 解析 `notification_channels` JSON（`[{type, severity_floor, url?}]`），按 `severity_floor` fan-out。`notify_risk_event` 内部映射 severity（`KILL_SWITCH` / `ORDER_PERSISTENCE_FAILED` → `CRITICAL`；`REJECTED` / `ORDER_FAILED` / `ORDER_TIMEOUT` / `DAILY_LOSS` → `WARNING`）。`_apply_credentials` 在凭证更新时重建 notifier。
- **券商韧性（P5+）**：`BrokerGateway._call_with_retry(fn, op=, max_retries=, base_ms=)` 包装 `submit_order` / `cancel_order` / `get_quote(s)` / `get_candlesticks`；每次重试写 `audit_logs.action=BROKER_RETRY`；业务拒绝（余额不足、非法 tick）不重试。

### 经纪商交互

- `BrokerGateway` 懒加载 `QuoteContext` / `TradeContext`。
- `get_quotes(symbols)` 批量报价；`get_candlesticks(symbol, period, count)` 供 `DataAggregator` / LLM。
- 单 symbol 行情订阅；RTH 内推送静默 ~90s 时 runner `_resubscribe_quotes_if_silent`；主动 `get_quote` 刷新设 `is_push=False` 以免误判推送存活。
- 测试通过 mock `BrokerGateway` 注入；`longport` 为可选依赖。

### 交易日历（market_calendar）

- `trade_day_for(market, instant)`：交易所本地**日历日**（不含节假日历）；`AppRunner` 注入 `RiskController` 与 `DailyPnlService.to_trade_day`。
- `is_trading_hours`：周末 + RTH 窗口；用于行情重订守卫，**不**阻止盘前盘后下单。

### LLM 顾问

- 服务：`llm_advisor_service.py`、`interval_application_service.py`；HTTP：`api/llm_advisor.py`；后台：`main._llm_analysis_cron`。
- `DataAggregator` 拉真实日 K / 1 分钟 K；`LLMAdvisorService(broker=...)` 可复用 runner 的 gateway。
- LONG 态允许 LLM **降低** `buy_low`（追价加仓）；见 Roadmap P7'。
- 节流：`_LAST_ANALYSIS_TIMESTAMP` 为 `time.monotonic()`；判断前检查 `<= 0` 避免冷启动误判。

### AppRunner / lifespan

- `main.lifespan`：`asyncio.to_thread(runner.start, loop=...)` / `to_thread(runner.stop)`，避免阻塞事件循环。
- `sync_today_orders_from_broker` 约 15s 节流；`GET /api/orders?refresh=true` 可 `force` 同步。

### 代码风格

- 后端：`basedpyright` basic；仓库根 `python3 -m basedpyright` 目标 0 errors（需本地 `pyrightconfig.json`，该文件可能在 `.gitignore`）。
- 优先 `X | None`；泛型别名上下文若 PEP 604 报错则改用 `Optional[X]`。
- 提交前：`pytest`、`basedpyright`、`npm run type-check`。

### 文档

- 用户向说明以 **`README.md`** 为准；迭代计划见 **`docs/Roadmap.md`**。
- 修改 API/路由/部署行为时，同步更新 README 与本文件。
- 已交付迭代的规格文档保存在 `docs/superpowers/specs/`；实施计划在 `docs/superpowers/plans/`。
- **子目录 AGENTS.md**（仅 `backend/app/domain/prompt/AGENTS.md`，涵盖 LLM 插件架构契约）。其他子目录知识已在根文档完整覆盖，不再重复。
- 当前下一迭代（P23 前端实时通知中心，Roadmap 建议拆 P23a WS 事件流 + P23b 通知中心 UI）。

- **期权/随机过程/鲁棒/路由/多元相依 V（P243–P252，参考 QuantLib BlackScholesProcess、Gatheral SVI、filterpy/pykalman、QuantLib StochasticProcess、Avellaneda-Lee、statsmodels.robust、SMPyBandits、statsmodels.lowess、Nautilus OrderRouting/FIX、pyvinecopulib/Bedford-Cooke-Aas）**：`_math_utils.py`（共享 `norm_cdf`（erf）/`norm_inv`（Acklam 逆正态）/`norm_pdf`，无 scipy，供 P243/P244/P246/P252 复用）+ `options_pricing.py`（Black-Scholes-Merton 1973 欧式 call/put 闭式 + Merton 连续分红 + 全 Greeks Δ/Γ/ν/Θ/ρ/vanna/volga，`OptionsResult`）+ `implied_volatility.py`（Brenner-Subrahmanyam 初值 + Newton-Raphson 反演 σ（bisection 回退 + 无套利边界校验）+ Gatheral 2004 raw-SVI 5 参原始参数化 `w(k)=a+b(ρ(k−m)+√((k−m)²+σ²))` Gauss-Newton/LM NLSQ 拟合（`a≥0,b≥0,|ρ|≤1,σ>0` 投影约束 + 5×5 高斯消元），`ImpliedVolResult`/`SviFit`）+ `kalman_filter.py`（线性 Kalman predict/update + Joseph 形式协方差稳定更新 + 静态/逐步矩阵 + 可选控制输入 B/u + 记录逐步 predicted mean/cov 供 RTS，`rts_smoother` Rauch-Tung-Striebel 1965 固定区间后向平滑，`KalmanResult`）+ `stochastic_processes.py`（GBM 精确 log-Euler + OU Euler-Maruyama + CIR 全截断/反射零 Euler + Merton 1976 复合泊松跳扩散（Box-Muller 正态 + Knuth 泊松，`random.Random(seed)` 确定性）+ 各过程解析矩 `E[ln S]`/`Var`/OU 平稳方差/CIR Feller 标志/Merton 跳计数，`ProcessResult`）+ `stat_arb_signals.py`（Gatev-Goetzmann-Rouwenhorst 2006 距离法 spread（归一化起价 1.0）+ Avellaneda-Lee 2008 z-score 进出场阈值（SHORT_SPREAD/LONG_SPREAD/FLAT + 滞回保持）+ 复用 P223 `half_life_ou` OU 半衰期，`StatArbResult`）+ `robust_statistics.py`（MAD `median(|x−median|)·1.4826` + winsorize α 分位裁剪 + α-trimmed mean + Theil 1950/Sen 1968 中位斜率回归（O(n²) 配对斜率）+ Huber 1964 M-estimator IRLS（MAD 尺度 + bisquare 风格权重）+ `RobustStatsResult` 聚合）+ `bandits.py`（ε-greedy / Auer 2002 UCB1 `mean+√(2 ln t/n_i)` / Thompson 1933 Beta 共轭（Marsaglia-Tsang Gamma 采样，shape<1 boosting）/ Thompson Gaussian 共轭后验 + `simulate` 伯努利/高斯奖励 + `regret` 累计后悔，`BanditResult`，全程 `random.Random(seed)`）+ `loess.py`（Cleveland 1979 LOWESS：tricube 核 `(1−|u|³)³` + k 近邻 + 加权 2×2 局部线性正规方程 + 鲁棒 bisquare 迭代重加权（残差/MAD 缩放）+ 零权重回退非鲁棒拟合，`LowessResult`）+ `smart_order_routing.py`（多 venue L1 最优价贪婪路由：buy 取最低有效 ask（`ask+fee`）/sell 取最高有效 bid（`bid−fee`）+ 逐 venue 深度上限拆单 + tick 量化（buy 向下/sell 向上）+ WAP/总费用/未成交余量，纯计算不接 broker，`VenueQuote`/`SorResult`）+ `vine_copula.py`（Bedford-Cooke 2001/Aas 2009 vine：C-vine（中心资产树，全配对）与 D-vine（链式相邻）level-1 逐对 copula 拟合，复用 P235 `kendall_tau`/`gumbel_fit`/`clayton_fit` + Gaussian `ρ=sin(πτ/2)` + 经验 CDF rank 伪观测 + 闭式 pair 对数似然 + AIC/BIC，`PairCopula`/`VineCopulaResult`）。**新增端点：** `POST /api/platform/options-pricing` + `POST /api/platform/implied-volatility`（`mode=iv`/`mode=svi`）+ `POST /api/platform/kalman-filter`（`smooth?`）+ `POST /api/platform/stochastic-processes` + `POST /api/platform/stat-arb-signals` + `POST /api/platform/robust-statistics` + `POST /api/platform/bandits` + `POST /api/platform/loess` + `POST /api/platform/smart-order-routing` + `POST /api/platform/vine-copula`。全部**零新依赖**、确定性、纯 Python（numpy 仍未引入；RNG 一律 `random.Random(seed)`），可选注入、默认路径零行为变更。
