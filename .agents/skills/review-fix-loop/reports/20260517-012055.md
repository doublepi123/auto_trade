# Review-Fix Loop 报告

- **目标**: whole-repo tracked source/config/test files
- **时间**: 2026-05-17 01:20:55 Asia/Shanghai
- **最大迭代次数**: 10
- **初始哈希**: `e9b02f645a34e71b168e62723a32dd38b6217c45ce284346d7752717a8d9dc05`
- **最终哈希**: `4fcaf6cc4e144c98c146767b6593e84055c5521bb6bf38398e9ac623cff67552`
- **总迭代次数**: 3
- **终止原因**: clean

## 迭代历史

### Iteration 1

**Review**: 发现 17 个问题

- [critical] `docker-compose.yaml` — Docker 部署未传入 `AUTO_TRADE_API_KEY`，后端鉴权会在推荐部署路径中被禁用
- [critical] `backend/app/api/auth.py` / `docker-compose.yaml` — 本地未设置 API key 时鉴权放开；Docker 部署需要 fail closed
- [high] `frontend/src/views/Credentials.vue` / `backend/app/services/credentials_service.py` — 凭证表单提示留空保留，但保存空字段会清空已保存凭证
- [high] `backend/app/runner.py` — 凭证重载时旧的 `LONGPORT_*` 环境变量可能残留
- [high] `backend/app/runner.py` — broker 返回 rejected/cancelled 时，策略仍可能记录订单并推进状态
- [high] `frontend/Dockerfile` — 缺少 `.dockerignore`，Docker build 可能复制宿主 `node_modules`
- [medium] `backend/app/core/broker.py` — 订阅失败后仍标记为已订阅，后续同 symbol 重试会短路
- [medium] `backend/app/core/broker.py` / `backend/app/runner.py` — 下单资金未按市场币种选择 USD/HKD
- [medium] `backend/app/runner.py` — 手动重启 runner 可能把已有 FastAPI 事件循环引用覆盖为 `None`
- [medium] `backend/app/main.py` — runner 启动失败路径使用未定义 `logger`
- [medium] `frontend/src/views/Dashboard.vue` — WebSocket 打开但无状态消息时轮询被永久停用
- [medium] `frontend/src/App.vue` — API key 保存后当前页面不立即重载受保护数据
- [medium] `frontend/src/App.vue` — header 缺少 flex 布局，菜单和按钮可能溢出
- [medium] `backend/app/schemas.py` — 策略可保存空白或无市场后缀 symbol
- [medium] `frontend/src/views/Dashboard.vue` / `backend/app/api/trade.py` — 账户接口失败时 UI 无法区分真实 0 资产和券商不可用
- [low] `frontend/cypress/e2e/strategy.cy.ts` — hash router 下测试访问 `/strategy`，可能覆盖错页面
- [low] `frontend/src/utils/labels.ts` — `PARTIAL_FILLED` 显示为未知状态

**Fix**: 修复 16 个问题

- `docker-compose.yaml`, `.env.example`, `README.md` — 后端容器要求 `AUTO_TRADE_API_KEY`，端口默认绑定 `127.0.0.1`，补充 `CREDENTIAL_MASTER_KEY` 文档
- `frontend/.dockerignore` — 排除 `node_modules`、构建产物和 Cypress 产物
- `backend/app/services/credentials_service.py`, `frontend/src/views/Credentials.vue` — 空凭证字段改为保留现值，前端只提交非空字段
- `backend/app/runner.py` — 记录 broker 返回状态，拒绝/取消订单不推进状态；按市场选择现金币种；保留事件循环引用
- `backend/app/core/broker.py` — 订阅成功后才更新订阅状态；`get_cash()` 支持目标币种
- `backend/app/main.py` — 定义模块 logger
- `backend/app/schemas.py` — 标准化并校验 symbol
- `backend/app/api/trade.py`, `frontend/src/views/Dashboard.vue`, `frontend/src/types/index.ts` — 账户响应加入 `available/error`，UI 显示账户不可用提示
- `frontend/src/App.vue`, `frontend/src/views/Dashboard.vue` — API key 更新后刷新数据和 WebSocket；WebSocket 无新鲜状态时继续 REST 轮询
- `frontend/src/api/index.ts`, `frontend/src/views/Dashboard.vue` — 增加解除紧急停止入口
- `frontend/src/utils/labels.ts`, `frontend/cypress/e2e/strategy.cy.ts` — 修复部分成交标签和 hash 路由测试路径
- Tests — 新增/补强部署配置、凭证保留、订阅失败、币种现金、订单拒绝、runner loop、账户不可用和 symbol 校验测试

**未完全修复**:

- [high] `backend/app/runner.py` — 完整的订单成交确认仍需要 broker 轮询或 webhook；本轮先阻断 rejected/cancelled 明确失败状态，保留后续专项工作。

### Iteration 2

**Review**: 发现 1 个问题

- [high] `backend/app/runner.py` — 第 1 轮凭证环境变量修复过度：空数据库凭证会清空 `.env` 中的 `LONGPORT_*` 凭证，破坏 fresh install。

**Fix**: 修复 1 个问题

- `backend/app/runner.py`, `backend/tests/test_runner_credentials.py` — 保存凭证优先；保存值为空时回退到 `settings.longbridge_*`；两边都空才清除进程环境变量。

### Iteration 3

**Review**: 未发现新的明确缺陷

- 前端第 2 轮复审 clean
- 后端/部署第 3 轮静态复查未发现由本次改动引出的新问题
- `git diff --check` clean

## 最终状态

- 所有发现的问题: 18
- 已修复: 17
- 无法修复: 1
- 循环检测触发: no

## 验证

- `python3 -m pytest tests/ -q` → `146 passed in 24.49s`
- `npm run build` → Vue typecheck and Vite production build succeeded
- Browser DOM check → 仪表盘、凭证设置、策略配置路由可打开，控制台无前端错误
- `git diff --check` → clean

## 最终建议

- 后续建议单独做一项订单状态同步：轮询 broker order status 或接 webhook，在确认成交后再更新风险 PnL 和最终订单状态。
- 本地开发仍允许空 `AUTO_TRADE_API_KEY` 并打印警告；Docker Compose 部署已改为必须显式设置 API key。
