# Review-Fix Loop 报告 — P43–P58 (diff:main)

- **目标**: diff:main (feature/p43-p58-autonomous-feature-iterations vs main)
- **时间**: 2026-06-16 11:00
- **最大迭代**: 5
- **是否分片并行**: yes (6 shards: A backtest-engine / B read-only-data / C journal-llm-timeline / D alerts-reports-presets-notif / E shared-data-layer / F frontend)
- **终止原因**: clean (1 轮：review → fix → 全局收敛 → 验证全绿)

## 迭代历史

### Iteration 1 — Review (6 shards 并行)
6 个 review subagent 并行审查，返回结构化 findings。过滤掉 false/accepted/out-of-scope 后，确认 **9 个真实问题**：

| # | 严重度 | 文件 | 问题 |
|---|--------|------|------|
| 1 | **high** | `alert_rule_service.py` evaluate | 单条规则异常中断整个 cron tick，跳过其余规则且不 commit |
| 2 | **medium** (×2 agents) | `backtest_run_service.py:83` | 兜底 `BacktestParams(buy_low=0,sell_high=0)` 违反 `gt=0` → 损坏 JSON 时 500 |
| 3 | **medium** | `AlertRules.vue` toggleEnabled | PUT `{...row}` 含 `id/last_fired_at/created_at` 命中 `extra="forbid"` → 开关永远 422 |
| 4 | low | `backtest_run_service.py:23` | 空白名 schema 通过后 strip 成 `""` |
| 5 | low | `models.py` AlertRule docstring | 写 `daily_loss_pct`，代码用 `daily_loss` |
| 6 | low | `position_pnl_service.py` | `total_pct` 恰为 0 时 None；有持仓无行情仍报 available |
| 7 | low | `runner.py:93` 默认 notifier | 无 sink（启动窗口通知不可审计） |
| 8 | low | `alert_rule_service._fetch_quotes` | 静默吞 broker 故障 |
| 9 | low | `Backtest.vue` heatmapCellStyle | 每格重算 min/max → O(n²) |

**已排除（false / pre-existing / accepted design）**：Strategy.vue 费率缩放（核验 backend 存分数，正确）；DecisionTimeline setTimeout / App.vue 未用绑定 / schemas step 死代码（既有非本次改动）；trade_note upsert TOCTOU（SQLite/GIL/单用户，accepted）；timeline total clamp / symbol 子串匹配（既有文档化取舍）。

### Fix（9 个均已应用）
1. evaluate 每规则体包 try/except + continue，尾部 commit 仍执行。
2. 兜底 params 改 `buy_low=1.0, sell_high=2.0`（满足 `gt=0`）。
3. `toggleEnabled` 构造干净 7 字段 payload。
4. `BacktestRunSaveRequest.name` 加 `field_validator` 拒绝空白（→ 422）。
5. AlertRule docstring 改 `daily_loss` + 注明阈值是带符号 PnL。
6. `total_pct` 去 `!=0` 子句；有持仓无行情一律 `available=False`。
7. 默认 notifier 注入 `sink=get_notification_sink().record`。
8. `_fetch_quotes` 失败时 `logger.warning(...exc_info)`。
9. heatmap 预计算 `heatmapRange` computed（O(1)/cell）。

### 全局收敛
修复均局部、互不冲突；`position_pnl` available 语义变更的消费者 `PositionPnlPanel` 已处理 `!available` 警告，Cypress fixture 不受影响。**无跨片破坏。**

## 验证（修复后全绿）
- 后端受影响分片：37 passed。
- **全量 pytest: 1091 passed, 2 pre-existing (test_watchlist_score), 1 skipped — 0 回归**。
- **basedpyright: 0/0/0**；**vue-tsc: 0**；**build pass**；chunk 预算不退步。

## 最终状态
- 发现: 9（+ 若干 false/pre-existing 已排除）
- 已修复: 9；无法修复: 0；循环检测触发: 否（单轮收敛）

## 最终建议
- trade_note 并发 upsert IntegrityError：单用户内网可接受；多写者改 `INSERT ... ON CONFLICT DO UPDATE`。
- `daily_loss` 阈值极性：UI「日内亏损 ≤」已表意；可加负值提示增强防呆。
- notification sink 单例未加锁：GIL+单进程良性；多进程改 lifespan eager init。
