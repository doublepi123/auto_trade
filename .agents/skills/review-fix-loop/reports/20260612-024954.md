# Review-Fix Loop 报告（第 4–10 轮）

- **目标**: 整个仓库所有代码（每轮独立 fresh review）
- **时间**: 2026-06-12
- **总迭代次数**: 10（第 1–3 轮见 `20260612-022101.md`）
- **终止原因**: 达到 10 轮上限
- **最终测试**: 834 passed，`vue-tsc` clean

## 各轮摘要

### Iteration 4
- Webhook 禁 redirect、DNS 校验强化、HK `next_session_open` 午休
- MACD 短数据返回 `{}`、timeout recovery 修复
- 通知/控制/图表竞态、nginx 360s 超时、compose health 依赖
- watchlist set-trading 审计、test_account_api PID 隔离

### Iteration 5
- LLM interval 改用 `apply_suggestion` + `_reload_strategy_after_save`
- 次要标的继承 primary StrategyParams
- broker sync 保留 SELL_SHORT/BUY_TO_COVER 语义 side
- repricing 除零防护

### Iteration 6–8（fresh review + 选择性修复）
- WS 瞬断可重连（非永久 polling）
- Dashboard 控制按钮 in-flight 防重复提交
- status history 请求序列号
- `is_trading_hours` 默认 false

### Iteration 9–10
- cancel 与 reconcile 共享 `_reconcile_in_flight` 防双计 PnL
- GET `/api/status` 乐观 ledger replay 守卫（对齐 runner）
- LLM cron 仅在 `success` 时消耗 symbol budget
- 测试对齐 LONG 态 anti-chasing 语义

## 第 10 轮仍存在的已知边界（未在本轮修复）

- 多市场 watchlist 的 daily PnL 仍按 primary market 日历
- pending timeout 最终 fallback 仍可能在 broker 侧留 orphan order（需更大重构）
- `event_list_service` 合并分页 2000 条上限
- `GET /api/orders?scope=today` 使用 UTC 日界
- WS query `api_key`、docker-compose LAN 暴露（设计取舍）

## 最终哈希

- 迭代 4 起点: `2ccce11b...`
- 第 10 轮后: 见 `git diff` / 工作区
