# Review-Fix Loop 报告

- **目标**: 整个项目 (`backend/app` + `frontend/src`)
- **时间**: 2026-06-11
- **总迭代次数**: 4
- **终止原因**: clean

## 迭代历史

### Iteration 1
**Review**: 发现 12 个问题

- [high] 部分读接口无鉴权（reports、review、indicators、performance、experiments 等）
- [high] Docker Hub 预构建前端无 API key
- [medium] 多标的仓位同步仅 primary engine
- [medium] LLM `_position_context` broker 失败时回退 primary engine
- [medium/low] reports 500 泄露异常、health 暴露运维信息等

**Fix**: 修复 8 项
- 路由级 `require_api_key()` 覆盖 reports/review/indicators/performance/experiments/strategy_experiments/watchlist
- LLM GET interactions/status 加鉴权
- `_position_context` 使用 symbol 专属 engine（兼容 fake runner）
- `_sync_engine_state_with_positions` 同步所有 symbol runtime
- reports 500 改为通用错误 + 日志
- docker-compose.dockerhub.yaml 添加前端 key 说明注释

**哈希**: `871e8a95…` → `026ef2bb…`

### Iteration 2
**Review**: 发现 8 个问题

- health 暴露 env/异常/risk
- 前端 backtest/experiment 10s 超时不足
- 错误信息泄露、csv 无大小限制等

**Fix**: 修复 7 项
- health 精简响应，异常仅记日志
- experiments create / order cancel 通用错误
- `csv_text` max 2M、`price_points` max 50k
- backtest 120s、strategy experiment 300s 超时
- llm-evaluations symbol 格式校验

### Iteration 3
**Review**: 发现 1 个问题

- LLM analyze 仍向客户端返回原始异常文本

**Fix**: analyze 客户端返回 `"LLM analysis failed"`，详细错误仅存 interaction 记录

### Iteration 4
**Review**: clean — 无新问题

## 最终状态

- **所有发现的问题**: ~21
- **已修复**: ~17
- **架构性权衡（未修复）**: 4
- **循环检测触发**: no

## 验证

```
cd backend && python3 -m pytest tests/ -q   # 824 passed
cd frontend && npm run type-check           # clean
```

## 架构性权衡（需知晓）

1. `VITE_AUTO_TRADE_API_KEY` 编译进 SPA bundle（内网部署可接受）
2. WebSocket 通过 `?api_key=` 鉴权（可能被代理日志记录）
3. Docker Hub 预构建前端镜像不含 API key
4. `/api/health` 保持无鉴权（Docker healthcheck 需要）
