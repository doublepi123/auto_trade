# Review-Fix Loop 报告

- **目标**: 整个仓库所有代码
- **时间**: 2026-06-12
- **总迭代次数**: 3（测试全绿且收敛后终止；未跑满 10 轮上限）
- **终止原因**: clean（测试 833 passed，收敛审查无 HIGH 残留）
- **是否分片并行**: yes
- **shard 数量**: 6

## Shard Plan

- shard A: `backend/app/core/` + `backend/app/domain/`
- shard B: `backend/app/services/` + `backend/app/runner.py`
- shard C: `backend/app/api/` + main/schemas/models/database/config
- shard D: `backend/tests/`
- shard E: `frontend/src/`
- shard F: `frontend/cypress/` + docker/vite config

## 迭代历史

### Iteration 1

**Review**: 6 个 shard 共发现 ~40 个问题（9+7+9+4+9+11）

**Fix**: 修复 28 项高/中优先级真实缺陷，包括：

- HK 午休时段 RTH 判定
- A/B `enroll_version` + `select_variant` 稳定排序
- Webhook SSRF 校验（`url_safety.py`）
- Broker 空 order_id 拒绝
- Engine LONG 态 LLM 加仓
- Runner 多标的 restore / pending broker 刷新 / LONG 持仓对账
- Trade execution restore 回调保留、timeout 部分成交、tracked 出场价
- Control/start 顺序与 pause 状态同步
- Indicators 按 symbol 推断 market
- 通知流移至 App.vue、WS 鉴权失败停重连、kill-switch 错误提示
- 测试断言修复、E2E 去 flaky sleep、Cypress stub/hash 修复

### Iteration 2

**Review**: 全局收敛发现 6 项残留

**Fix**:

- Start 后从 `runner.risk` 同步 pause 到 DB
- Push quote 无 bid/ask 时跳过 spread 警告
- Timeout 路径处理 PARTIAL_FILLED 部分成交
- `event_list_service` 扩展 severity 映射
- Webhook DNS 解析 SSRF 防护（best-effort）

### Iteration 3

**Review**: 收敛审查发现 url_safety 误吞 private IP 拒绝、timeout 双重 finalize

**Fix**:
- 分离 `ip_address` 解析与 `_reject_ip` 的 ValueError 处理
- 移除 timeout 路径重复 finalize；cancel 失败时 recovery 查询部分成交
- 测试 webhook URL 改用公网 literal IP（规避 WSL DNS 返回 198.18.x）

## 最终状态

- 基线哈希: `90855a7f...`
- 修复后哈希: `32297da2...`
- 所有发现的问题: ~40
- 已修复: 32+
- 无法修复/刻意保留: 若干低优先级（requirements 未 pin、docker-compose LAN 暴露为内网设计、WS query api_key 兼容、/api/status 副作用读等）
- 循环检测触发: no
- 测试: **833 passed**, `vue-tsc` clean

## 最终建议

- 若部署到不可信网络：评估移除 WS query `api_key`、收紧 docker-compose 默认绑定
- A/B 多 variant 需调用新 `enroll_version` API（`activate_version` 仍为单 variant 独占）
- 可选后续：`GET /api/status` 去副作用写、credentials GET 脱敏 webhook URL
