# Review-Fix Loop 报告

- **目标**: `backend/app` 全量代码库 (~19,500 行, ~80 文件)
- **时间**: 2026-06-15
- **总迭代次数**: 1 (Round 1 fix + 1 次全局收敛 review)
- **终止原因**: clean (全局收敛 review backend 层面 clean)
- **是否分片并行**: yes
- **shard 数量**: 12

## Shard Plan

| Shard | 范围 | 行数 |
|---|---|---|
| A | core/{engine,risk,fees,audit,backtest}.py | ~1133 |
| B | core/{broker,market_calendar,holiday_calendar,url_safety}.py | ~1422 |
| C | core/{credential_crypto,notify}.py + notifiers/* | ~946 |
| D | runner.py + main.py | ~2877 |
| E | services/trade_execution_service.py | ~1497 |
| F | services/llm_*.py | ~1312 |
| G1 | services/{daily_pnl,data_aggregator,event_list,report,review,watchlist_score}_service.py | ~1958 |
| G2 | services/{credentials,interval_application,runtime_state,strategy_experiment,strategy,trade_event,watchlist,experiment_grid}_service.py | ~1582 |
| H | domain/* (analysis, experiment, performance, prompt, sentiment) | ~1449 |
| I1 | api/{trade,llm_advisor,strategy,strategy_experiments,experiments}.py | ~1909 |
| I2 | api/{backtest,calendar,credentials,indicators,metrics,performance,reports,review,watchlist,ws,auth,deps}.py | ~1349 |
| J | config.py, database.py, models.py, schemas.py | ~2084 |

## 验证结果

| 检查项 | 结果 |
|---|---|
| pytest 全量 | **951 passed**, 2 pre-existing failures (watchlist_score LLM 环境), 1 skipped |
| basedpyright (app/) | 84 errors (全部为 import resolution 环境问题) + 7 pre-existing 类型错误, **0 新引入** |
| vue-tsc (frontend) | **clean** (前端 ReportMetrics 类型已同步 nullable) |
| 回归检测 | test_runner_credentials 回归 (risk.py None guard) 已发现并修复 |

## 修复明细 (33 issues)

### Critical (3)
| ID | 文件 | 描述 | 处理 |
|---|---|---|---|
| E1 | trade_execution_service.py:38 | HKEX tick table 用过期 Phase 1 规则 (2018), Phase 2 (2019-07) 已生效 7 年 — 港股订单可能被拒单 | 修复为 Phase 2 (20-100 合并为 0.050) |
| J1 | database.py:178 | 迁移回填 daily_pnl_date 时错误重置 daily_pnl=0, consecutive_losses=0 — 升级旧 DB 静默清空 P&L/暂停状态 | 移除重置, 只回填日期 |
| B1 | broker.py:565 | submit_limit_order 无 _call_with_retry | **by design** — 加注释说明 (下单 non-idempotent, 重试有双倍仓位风险) |

### High (5)
| ID | 文件 | 描述 |
|---|---|---|
| B2 | broker.py:25 | SDK 回退 RETRYABLE_EXC=(Exception,) 过宽 → 收窄为 (OSError,ConnectionError,TimeoutError) |
| C1 | webhook.py:174 | webhook URL (含嵌入凭证) 完整写入日志 → 改为只记录 hostname |
| F1 | llm_advisor_service.py:352 | 节流 TOCTOU 竞态 (并发绕过 60s cooldown) → reserve slot 模式 |
| G2-1 | credentials_service.py:154 | notification_channels 字符串路径跳过逐渠道验证 (可注入恶意 webhook) → 提取共用 _validate_channel |
| A1 | backtest.py:360 | Calmar ratio docstring 声称 "Annualised" 但未年化 → 修正 docstring |

### Medium (16)
A2 (Sortino 分母), A3 (SHORT 非对称注释), A4 (fees warning), B3 (bare except 收窄), B4 (enum 回退), D1/D2/D3 (_mark_fill_processed 缺 symbol), F2 (_escape_orphan_braces), G1-1 (max_loss None), G1-2 (price 回退 warning), G1-3 (unclosed remainder warning), G2-2 (guardrail 清字段), I1-1 (StrategyResponse consistency_warnings), I1-2 (cancel_order DB 失败处理), I1-3 (current_price None guard), I2-1 (metrics ORM 修改→tuple), I2-2 (credentials 异常泄露)

### Low (9)
A5 (risk 死代码→**回滚**: is not None 是必要守卫), B5 (注释年份), B6 (退避 2x), B7 (IPv6 mapped 检查), D4 (SHORT 对账注释), D7 (同 D4), G1-4 (死代码移除), I2-3 (ws WebSocketDisconnect), + 前端类型同步

## 推迟的 Issues (7, 架构性/配置化)

| ID | 原因 |
|---|---|
| A6 | risk.py:85 冗余防御检查 — 保留为防御性代码 |
| D5 | SQLite 并发写入 — 需要 queue+单消费者架构改动 |
| D6 | LLM 预算计数偏差 (±1) — 边缘场景 |
| G1-5 | watchlist rationale 正则转义引号 — LLM 输出边缘 |
| G1-6 | data_aggregator del market — 代码异味 |
| G1-7 | review_service 魔数 -1000 — 配置化改动 |
| I1-info | 前端 Strategy.vue 未消费 consistency_warnings — 后续 UI 迭代 |

## 最终状态
- 所有发现问题: 40
- 已修复: 33 (含 1 个 false positive 回滚 + 1 个前端类型同步)
- 推迟: 7
- 循环检测触发: no
- 回归: 1 (risk.py None guard) 已修复
- 测试新增/更新: 19 个测试文件

## 关键经验
1. **False positive 风险**: oracle 标记 risk.py `is not None` 为 "死代码" (A5), 实际是必要的 None 守卫 (runtime_state_service 传入 None). 删除后引入 test_runner_credentials 回归. **已回滚并加注释**.
2. **Subagent 文件同步问题**: 4 个 fixer (C/E/I2/J) 声称完成修改但未落地到磁盘. 主 agent 手动修复了这 4 个文件 (含 2 个 critical). **教训: fixer 完成后必须验证文件实际变更 (git status/diff)**.
3. **交易系统安全权衡**: submit_limit_order 不重试是正确的防御设计 (B1). 下单 non-idempotent, 网络重试有双倍仓位风险.
