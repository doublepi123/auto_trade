# Review-Fix Loop 报告

- **目标**: 全代码库（backend + frontend）
- **时间**: 2026-06-06 01:00:00
- **总迭代次数**: 1
- **终止原因**: clean（全局收敛 Review 通过）
- **是否分片并行**: yes
- **shard 数量**: 4

## Shard Plan
- **Shard A: Backend Core** — `backend/app/core/` (broker, engine, risk, audit, notifiers, crypto, calendar, backtest)
- **Shard B: Backend Services** — `backend/app/services/` (11 files)
- **Shard C: Backend API+Main** — `backend/app/api/`, `main.py`, `runner.py`, `config.py`, `models.py`, `schemas.py`, `database.py`
- **Shard D: Frontend** — `frontend/src/` (views, components, composables, api, types)

## 迭代历史

### Iteration 1 — Review
**4 个并行 Review Agent，共发现 30 个问题**

| 分片 | Issues | High | Medium | Low |
|------|--------|------|--------|-----|
| A: Backend Core | 8 | 1 | 5 | 2 |
| B: Backend Services | 8 | 2 | 4 | 2 |
| C: Backend API+Main | 7 | 3 | 3 | 1 |
| D: Frontend | 7 | 2 | 3 | 2 |
| **Total** | **30** | **8** | **15** | **7** |

#### High Issues (8)
1. **backtest.py:144** — BacktestEngine 未在日切换时重置 consecutive_losses，导致回测比实盘更悲观
2. **daily_pnl_service.py:82** — 查询窗口仅用 created_at，遗漏 >1 天前提交但当日成交的订单，导致 PnL 错误
3. **event_list_service.py:136** — source='all' 时 total 报告真实总数但结果上限 2000，深分页返回空页
4. **runner.py:855** — 多 symbol 场景下 quote 失败回退用主引擎价格，可能以错误价格执行交易
5. **main.py:289** — LLM 订单执行同步阻塞事件循环
6. **trade.py:174** — filled_at 被设置在所有终态订单（含 REJECTED/CANCELLED）上
7. **useStatusStream.ts:63** — WebSocket onerror 后 onclose 仍调用 scheduleReconnect()，导致无限重连
8. **NotificationSettings.vue:6** — useNotificationStream 非单例，偏好设置不同步

#### Medium Issues (15)
1. **broker.py:547** — getattr(OrderType, 'LO') 无默认值，SDK 变更会崩溃
2. **broker.py:716** — close() 仅捕获 AttributeError/TypeError，可能泄漏 trade context
3. **broker.py:463** — get_quotes 在返回数量不匹配时抛 RuntimeError，过于严格
4. **broker.py:684** — `or` 链把 0 当作"未找到"，可能返回错误价格
5. **credential_crypto.py:76** — PBKDF2 使用静态 salt，降低防御深度
6. **trade_execution_service.py:1168** — avg_price 不匹配阈值硬编码 $2，跨价格区间不可靠
7. **strategy_service.py:74** — 死代码：sct_key 检查永远为 False
8. **runtime_state_service.py:114** — persist_symbol 用全新 RiskController()，快照中风险状态全为默认值
9. **interval_application_service.py:217** — 除以 current_price 前未检查零值
10. **llm_advisor.py:182** — analyze 端点缺少 auth 依赖（preview 有）
11. **llm_advisor.py:391** — enable/disable LLM 端点无审计日志
12. **trade.py:221** — get_orders 全量加载后 Python 过滤，性能差
13. **useNotificationStream.ts:15** — soundEnabled 定义但从未生效
14. **useNotificationStream.ts:92** — eventKey 函数死代码
15. **Review.vue:260** — Promise.all 中任一失败导致全部结果丢失

### Iteration 1 — Fix
**4 个并行 Fix Agent，修复 24 个问题（high + medium）**

#### Shard A Fixes (6)
| # | 文件 | 修改摘要 |
|---|------|---------|
| 1 | backtest.py | 日切换时重置 consecutive_losses=0，清除连续亏损暂停原因 |
| 2 | broker.py:547,548,772 | getattr 添加默认值 "LO"/"DAY" |
| 3 | broker.py:716 | close() 改为捕获 Exception |
| 4 | broker.py:463 | get_quotes 返回 0 时仍抛异常，部分结果时 warning |
| 5 | broker.py:684 | or 链改为显式 None 检查 |
| 6 | credential_crypto.py | 每安装独立 salt + 旧 salt 向后兼容回退 |

#### Shard B Fixes (6)
| # | 文件 | 修改摘要 |
|---|------|---------|
| 1 | daily_pnl_service.py | 查询窗口增加 filled_at 条件（or_） |
| 2 | event_list_service.py | total 封顶为 min(real_total, MAX_MERGED_FETCH) |
| 3 | trade_execution_service.py | 阈值改为相对 2% |
| 4 | strategy_service.py | 删除死代码 sct_key 检查 |
| 5 | runtime_state_service.py | persist_symbol 接受可选 risk 参数 |
| 6 | interval_application_service.py | current_price <= 0 提前返回 |

#### Shard C Fixes (6)
| # | 文件 | 修改摘要 |
|---|------|---------|
| 1 | runner.py | _quote_for_llm_order 从 _symbol_runtimes 查找正确 engine |
| 2 | main.py | execute_llm_order_decision 包装在 asyncio.to_thread 中 |
| 3 | trade.py:174 | filled_at 仅在 status=="FILLED" 时设置 |
| 4 | llm_advisor.py:182 | 添加 dependencies=[Depends(require_api_key())] |
| 5 | llm_advisor.py:391 | enable/disable 添加审计日志 |
| 6 | trade.py:221 | 改为 DB 级 offset/limit 分页 |

#### Shard D Fixes (4)
| # | 文件 | 修改摘要 |
|---|------|---------|
| 1 | useStatusStream.ts | 添加 wsErrorOccurred 标志，onerror 后 onclose 不再重连 |
| 2 | useNotificationStream.ts | 改为模块级单例 + refCount 引用计数 |
| 3 | useNotificationStream.ts | 实现 soundEnabled 音频播放 + 删除 eventKey 死代码 |
| 4 | Review.vue | Promise.all → Promise.allSettled，部分失败优雅处理 |

#### 额外修复
| 文件 | 修改摘要 |
|------|---------|
| tests/test_api.py:629 | 更新断言：CANCELLED 订单的 filled_at 应为 None |

### Iteration 1 — 全局收敛 Review
**结果: ✅ CLEAN** — 所有 16 个修改文件审查通过，无回归，修改间一致。

## 测试结果
- **后端**: 672 passed, 1 skipped, 1 pre-existing deselected（test_llm_advisor AB 变体测试预存失败）
- **前端**: type-check 通过
- **预存问题**（非本次引入）:
  - `test_llm_advisor::test_select_variant_returns_template_from_existing_version` — 预存失败
  - `test_e2e_restart.py` — 预存挂起

## 哈希追踪
| 阶段 | Backend Hash | Frontend Hash |
|------|-------------|---------------|
| 基线 | 32676382... | 3d442f34... |
| 修复后 | 987d7839... | 64010ce2... |
| 循环检测 | ❌ 无循环 | ❌ 无循环 |

## 最终状态
- 所有发现的问题: 30
- 已修复: 25（24 high/medium + 1 测试断言）
- 未修复（low，暂不处理）: 7
- 循环检测触发: no

## 未修复的 Low Issues
1. market_calendar.py:43 — is_rth 包含精确收盘时间（边界条件）
2. broker.py:37 — 重试标记 '429'/'connection' 可能误匹配
3. credentials_service.py:95 — model_dump 假设非 dict 有 Pydantic 方法
4. llm_advisor_service.py:315 — 分析和交互记录用不同 DB session
5. trade.py:66 — _is_today 用本地时区而非 UTC
6. useStatusStream.ts:92 — WebSocket 连接时仍运行 polling
7. Dashboard.vue:667 — 4 个 API 调用每 3 秒同时触发

## 最终建议
- 7 个 low 级别问题可在后续迭代中处理
- 2 个预存测试问题需单独调查修复
- credential_crypto.py 的 salt 变更需要在部署文档中说明：首次部署会生成新 salt，旧部署自动兼容（legacy 回退）
